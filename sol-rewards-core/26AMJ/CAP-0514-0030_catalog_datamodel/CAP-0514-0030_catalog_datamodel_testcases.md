# Reward Catalogue Listing API (CQRS MongoDB Read Model)

## Metadata

| Field | Value |
|-------|-------|
| Ticket | [CAP-0514-0030](https://capillarytech.atlassian.net/browse/CAP-0514-0030) |
| Description | CQRS MongoDB read model for `GET /core/v1/rewards/catalogue` — replaces MySQL listing queries with MongoDB `active_rewards` + `rewards_catalogue` collections, fed via RabbitMQ batch listener, cached in Redis at 30s TTL |
| Module | sol-rewards-core |
| Developer / Owner | |
| Reviewer | |
| Date | 2026-05-14 |
| Pull request | |
| Releases detailing | |
| Preconditions | MongoDB 5.0 container running; MySQL container with rewards_utf8_db seed; RabbitMQ container; Redis container — all provisioned by `BaseIntegrationTest` via Testcontainers |
| Security Impact | NA |

---

## Test Cases

| # | Test Case | Test Steps | Type | Priority | Expected Result | Actual Result | Dev | Nightly | Staging | Prod | Comments |
|---|-----------|------------|------|----------|-----------------|---------------|-----|---------|---------|------|----------|
| 1 | CursorToken encode→decode round-trip preserves pointsValue and id | **Prerequisites:**<br>1. `CursorToken` class under test — no mocks, pure function<br>2. Test constants: `POINTS_VALUE = 500.0`, `ID_HEX = "507f1f77bcf86cd799439011"`<br><br>**Steps:**<br>1. Call `String token = CursorToken.encode(POINTS_VALUE, ID_HEX)`<br>2. Call `CursorToken.Decoded result = CursorToken.decode(token)`<br>3. Assert `result.getPointsValue() == POINTS_VALUE`<br>4. Assert `result.getId().equals(ID_HEX)` | Unit | P0 | • `token` is a non-null, non-empty base64 string<br>• `result.getPointsValue()` equals `500.0`<br>• `result.getId()` equals `"507f1f77bcf86cd799439011"`<br>• No exception thrown | | - | - | - | - | UT-01 · FR-005 |
| 2 | CursorToken.decode with non-base64 input throws exception | **Prerequisites:**<br>1. `CursorToken` under test — no mocks<br><br>**Steps:**<br>1. Call `CursorToken.decode("not-base64!!!")` inside `assertThrows(Exception.class, ...)`<br>2. Capture thrown exception | Unit | P0 | • An exception is thrown (IllegalArgumentException, JsonParseException, or custom CursorDecodeException)<br>• The caller (service layer) must be able to catch this and return HTTP 400<br>• No NPE or untyped RuntimeException that would yield a 500 | | - | - | - | - | UT-02 · FR-012 |
| 3 | CursorToken.decode with empty string throws exception | **Prerequisites:**<br>1. `CursorToken` under test — no mocks<br><br>**Steps:**<br>1. Call `CursorToken.decode("")` inside `assertThrows(Exception.class, ...)` | Unit | P1 | • Exception is thrown<br>• Not a NullPointerException | | - | - | - | - | UT-03 · FR-012 |
| 4 | CursorToken.decode with valid base64 but invalid JSON throws exception | **Prerequisites:**<br>1. `CursorToken` under test — no mocks<br>2. Prepare `String badJson = Base64.getEncoder().encodeToString("{not_valid_json".getBytes())`<br><br>**Steps:**<br>1. Call `CursorToken.decode(badJson)` inside `assertThrows(Exception.class, ...)` | Unit | P1 | • Exception is thrown (JsonParseException or similar)<br>• Exception message indicates JSON parsing failure<br>• Not a silent `null` return | | - | - | - | - | UT-04 · FR-012 |
| 5 | RewardCatalogueResource rejects pointsExact combined with pointsMin | **Prerequisites:**<br>1. `RewardCatalogueResource` under test with `@ExtendWith(MockitoExtension.class)`<br>2. `@Mock AuthDetailsProvider authDetailsProvider` — configure `authDetailsProvider.getAuthDetails().getOrgId()` → `1L`<br>3. `@Mock RewardCatalogueService catalogueService`<br>4. `@InjectMocks RewardCatalogueResource resource`<br><br>**Steps:**<br>1. Build `CatalogueListRequest` with `pointsExact=300.0`, `pointsMin=100.0`, `pointsMax=null`<br>2. Call `Response response = resource.list(request)`<br>3. Capture response status | Unit | P0 | • `response.getStatus()` equals `400`<br>• `catalogueService.list(any())` is **never** invoked (verified via `verify(catalogueService, never()).list(any())`) | | - | - | - | - | UT-05 · FR-011 |
| 6 | RewardCatalogueResource rejects pointsExact combined with pointsMax | **Prerequisites:**<br>1. Same setup as test case #5<br><br>**Steps:**<br>1. Build `CatalogueListRequest` with `pointsExact=300.0`, `pointsMin=null`, `pointsMax=500.0`<br>2. Call `Response response = resource.list(request)` | Unit | P0 | • `response.getStatus()` equals `400`<br>• `catalogueService.list(any())` never invoked | | - | - | - | - | UT-06 · FR-011 |
| 7 | RewardCatalogueResource valid request with no filters returns 200 and delegates to service | **Prerequisites:**<br>1. Same setup as test case #5<br>2. `when(catalogueService.list(any())).thenReturn(CatalogueListResponse.empty())`<br><br>**Steps:**<br>1. Build `CatalogueListRequest` with all filter fields null, no cursor<br>2. Call `Response response = resource.list(request)`<br>3. Assert status and verify service call | Unit | P0 | • `response.getStatus()` equals `200`<br>• `catalogueService.list(any())` called exactly once<br>• Response entity is the `CatalogueListResponse` returned by mock | | - | - | - | - | UT-07 · FR-001 |
| 8 | RewardCatalogueResource always uses orgId from authDetailsProvider, never from query param | **Prerequisites:**<br>1. Same setup as test case #5<br>2. `when(authDetailsProvider.getAuthDetails().getOrgId()).thenReturn(99L)`<br>3. `ArgumentCaptor<CatalogueListRequest> captor = ArgumentCaptor.forClass(CatalogueListRequest.class)`<br><br>**Steps:**<br>1. Build `CatalogueListRequest` (orgId field ignored if present in request DTO)<br>2. Call `resource.list(request)`<br>3. `verify(catalogueService).list(captor.capture())`<br>4. Assert captured request orgId | Unit | P0 | • `captor.getValue().getOrgId()` equals `99L`<br>• No way for caller to override orgId through query params (security guard verified) | | - | - | - | - | UT-08 · FR-001 |
| 9 | RewardCatalogueService returns cached response on cache HIT without querying MongoDB | **Prerequisites:**<br>1. `RewardCatalogueService` under test with `@ExtendWith(MockitoExtension.class)`<br>2. `@Mock CatalogueCache catalogueCache`<br>3. `@Mock ActiveRewardMongoRepository activeRewardRepo`<br>4. `@Mock RewardCatalogueMongoRepository catalogueRepo`<br>5. `@InjectMocks RewardCatalogueService service`<br>6. Prepare `CatalogueListResponse cached = new CatalogueListResponse(List.of(sampleItem), 1, null, false)`<br>7. Serialize to JSON string: `String cachedJson = objectMapper.writeValueAsString(cached)`<br>8. `when(catalogueCache.get(any())).thenReturn(Optional.of(cachedJson))`<br><br>**Steps:**<br>1. Build `CatalogueListRequest` with `orgId=1L`, no filters, no cursor<br>2. Call `CatalogueListResponse result = service.list(request)` | Unit | P0 | • `activeRewardRepo.findFiltered(any())` is **never** invoked<br>• `result.getItems()` contains the same item as the cached response<br>• `result.getTotal()` equals `1` | | - | - | - | - | UT-09 · FR-006 |
| 10 | RewardCatalogueService queries MongoDB on cache MISS and populates cache | **Prerequisites:**<br>1. Same setup as test case #9<br>2. `when(catalogueCache.get(any())).thenReturn(Optional.empty())`<br>3. Prepare `List<ActiveRewardDoc> docs = List.of(doc1, doc2)` (2 docs with rewardId=10L, 20L)<br>4. `when(activeRewardRepo.findFiltered(any())).thenReturn(docs)`<br>5. Prepare matching `List<RewardCatalogueDoc>` returned by `catalogueRepo.findByOrgIdAndRewardIdIn()`<br>6. `ArgumentCaptor<String> cacheCaptor = ArgumentCaptor.forClass(String.class)`<br><br>**Steps:**<br>1. Call `CatalogueListResponse result = service.list(request)`<br>2. `verify(catalogueCache).put(any(), cacheCaptor.capture())` | Unit | P0 | • `activeRewardRepo.findFiltered(any())` called exactly once<br>• `catalogueCache.put(any(), any())` called exactly once<br>• `cacheCaptor.getValue()` is valid JSON (not null, not empty)<br>• `result.getItems().size()` equals `2` | | - | - | - | - | UT-10 · FR-006 |
| 11 | RewardCatalogueService returns empty response for org with no active rewards | **Prerequisites:**<br>1. Same setup as test case #10<br>2. `when(catalogueCache.get(any())).thenReturn(Optional.empty())`<br>3. `when(activeRewardRepo.findFiltered(any())).thenReturn(Collections.emptyList())`<br><br>**Steps:**<br>1. Call `CatalogueListResponse result = service.list(request)` | Unit | P0 | • `result.getItems()` is empty (size 0)<br>• `result.getTotal()` equals `0`<br>• `result.getNextCursor()` is `null`<br>• No exception thrown | | - | - | - | - | UT-11 · FR-001 |
| 12 | RewardCatalogueService sets hasMore=true and trims list when MongoDB returns limit+1 docs | **Prerequisites:**<br>1. Default page size assumed: `limit=5`<br>2. Mock `activeRewardRepo.findFiltered()` returning `6` docs (limit + 1)<br>3. `when(catalogueCache.get(any())).thenReturn(Optional.empty())`<br><br>**Steps:**<br>1. Build request with `limit=5`, no cursor<br>2. Call `CatalogueListResponse result = service.list(request)` | Unit | P0 | • `result.getItems().size()` equals `5` (trimmed from 6)<br>• `result.getNextCursor()` is not null<br>• `result.isHasMore()` is `true` (or equivalent flag) | | - | - | - | - | UT-12 · FR-005 |
| 13 | RewardCatalogueService sets hasMore=false when MongoDB returns exactly limit docs | **Prerequisites:**<br>1. Mock `activeRewardRepo.findFiltered()` returning exactly `5` docs<br>2. `when(catalogueCache.get(any())).thenReturn(Optional.empty())`<br><br>**Steps:**<br>1. Build request with `limit=5`<br>2. Call `CatalogueListResponse result = service.list(request)` | Unit | P0 | • `result.getItems().size()` equals `5`<br>• `result.getNextCursor()` is `null`<br>• `result.isHasMore()` is `false` | | - | - | - | - | UT-13 · FR-005 |
| 14 | RewardCatalogueService passes groupIds filter to ActiveRewardMongoRepository | **Prerequisites:**<br>1. Same base setup<br>2. `ArgumentCaptor<CatalogueListRequest> captor`<br>3. `when(catalogueCache.get(any())).thenReturn(Optional.empty())`<br>4. `when(activeRewardRepo.findFiltered(captor.capture())).thenReturn(List.of())`<br><br>**Steps:**<br>1. Build request with `groupIds=[1L, 2L]`<br>2. Call `service.list(request)` | Unit | P1 | • `captor.getValue().getGroupIds()` contains exactly `[1L, 2L]`<br>• `activeRewardRepo.findFiltered()` called once | | - | - | - | - | UT-14 · FR-001 |
| 15 | RewardCatalogueService passes labelIds filter to ActiveRewardMongoRepository | **Prerequisites:**<br>1. Same base setup with `ArgumentCaptor`<br><br>**Steps:**<br>1. Build request with `labelIds=[10L, 20L]`<br>2. Call `service.list(request)` | Unit | P1 | • `captor.getValue().getLabelIds()` contains exactly `[10L, 20L]` | | - | - | - | - | UT-15 · FR-001 |
| 16 | ActiveRewardMongoRepository builds orgId as first Criteria predicate | **Prerequisites:**<br>1. `ActiveRewardMongoRepository` under test<br>2. `@Mock MongoTemplate mongoTemplate`<br>3. `ArgumentCaptor<Query> queryCaptor = ArgumentCaptor.forClass(Query.class)`<br>4. `when(mongoTemplate.find(queryCaptor.capture(), eq(ActiveRewardDoc.class), eq("active_rewards"))).thenReturn(List.of())`<br><br>**Steps:**<br>1. Build `CatalogueListRequest` with `orgId=1L`, no filters, no cursor<br>2. Call `repo.findFiltered(request)`<br>3. Inspect `queryCaptor.getValue()` | Unit | P0 | • The first `Criteria` in the captured `Query`'s criteria chain targets field `orgId`<br>• Value matches `1L`<br>• Verified by parsing `queryCaptor.getValue().getQueryObject().toBsonDocument()` — `orgId` is the leading key | | - | - | - | - | UT-16 · NFR-002 |
| 17 | ActiveRewardMongoRepository applies maxTimeMS(100) to find query | **Prerequisites:**<br>1. Same setup as test case #16<br><br>**Steps:**<br>1. Call `repo.findFiltered(request)`<br>2. Inspect `queryCaptor.getValue().getMeta().getMaxTimeMsec()` | Unit | P0 | • `query.getMeta().getMaxTimeMsec()` equals `100L`<br>• `maxTimeMS` constraint present on every `findFiltered()` call regardless of filters | | - | - | - | - | UT-17 · NFR-003 |
| 18 | ActiveRewardMongoRepository builds correct $or cursor predicate for continuation queries | **Prerequisites:**<br>1. Same setup as test case #16<br>2. Encode cursor: `String cursor = CursorToken.encode(200.0, "507f1f77bcf86cd799439011")`<br><br>**Steps:**<br>1. Build request with `orgId=1L`, `cursor=cursor`<br>2. Call `repo.findFiltered(request)`<br>3. Parse `queryCaptor.getValue().getQueryObject()` | Unit | P0 | • Query document contains `$or` array<br>• First element: `{pointsValue: {$gt: 200.0}}`<br>• Second element: `{pointsValue: 200.0, _id: {$gt: ObjectId("507f1f77bcf86cd799439011")}}`<br>• No cursor predicate present when cursor is null (first page) | | - | - | - | - | UT-18 · FR-005 |
| 19 | ActiveRewardMongoRepository includes groupIds $in filter when groupIds provided | **Prerequisites:**<br>1. Same setup as test case #16<br><br>**Steps:**<br>1. Build request with `orgId=1L`, `groupIds=[5L, 6L]`<br>2. Call `repo.findFiltered(request)`<br>3. Inspect captured query | Unit | P1 | • Query document contains `groupIds: {$in: [5, 6]}`<br>• When `groupIds` is null or empty, no `groupIds` predicate in query | | - | - | - | - | UT-19 · FR-001 |
| 20 | ActiveRewardMongoRepository includes labelIds $in filter when labelIds provided | **Prerequisites:**<br>1. Same setup as test case #16<br><br>**Steps:**<br>1. Build request with `orgId=1L`, `labelIds=[10L, 11L]`<br>2. Call `repo.findFiltered(request)` | Unit | P1 | • Query document contains `labelIds: {$in: [10, 11]}`<br>• When `labelIds` is null or empty, no `labelIds` predicate | | - | - | - | - | UT-20 · FR-001 |
| 21 | ActiveRewardMongoRepository.bulkUpsert calls BulkOperations.upsert with orgId+rewardId filter per doc | **Prerequisites:**<br>1. `ActiveRewardMongoRepository` under test<br>2. `@Mock MongoTemplate mongoTemplate`<br>3. `@Mock BulkOperations bulkOps`<br>4. `when(mongoTemplate.bulkOps(BulkOperations.BulkMode.UNORDERED, "active_rewards")).thenReturn(bulkOps)`<br>5. `when(bulkOps.upsert(any(), any())).thenReturn(bulkOps)`<br>6. `when(bulkOps.execute()).thenReturn(mock(BulkWriteResult.class))`<br>7. `ArgumentCaptor<Query> filterCaptor = ArgumentCaptor.forClass(Query.class)`<br>8. Prepare `List<ActiveRewardDoc> docs = List.of(docWithOrgId1RewardId10, docWithOrgId1RewardId20)`<br><br>**Steps:**<br>1. Call `repo.bulkUpsert(docs)`<br>2. `verify(bulkOps, times(2)).upsert(filterCaptor.capture(), any())` | Unit | P0 | • `bulkOps.upsert()` called exactly twice (once per doc)<br>• First captured filter query contains `orgId=1L` AND `rewardId=10L`<br>• Second captured filter query contains `orgId=1L` AND `rewardId=20L`<br>• `bulkOps.execute()` called exactly once | | - | - | - | - | UT-21 · FR-001 |
| 22 | RewardCatalogueSyncListener processes mixed-org batch with per-org MySQL queries (never cross-org) | **Prerequisites:**<br>1. `RewardCatalogueSyncListener` under test<br>2. `@Mock RewardRepository rewardRepository`<br>3. `@Mock ActiveRewardMongoRepository activeRewardRepo`<br>4. `@Mock RewardCatalogueMongoRepository catalogueRepo`<br>5. `@Mock CatalogueCache catalogueCache`<br>6. Prepare batch: `List<String> messages = List.of(toJson(orgId=1L, rewardId=10L, UPSERT), toJson(orgId=2L, rewardId=20L, UPSERT))`<br>7. Mock `rewardRepository.findByOrgIdAndIdIn(1L, [10L])` → list with reward for org 1<br>8. Mock `rewardRepository.findByOrgIdAndIdIn(2L, [20L])` → list with reward for org 2<br><br>**Steps:**<br>1. Call `listener.onBatch(messages)`<br>2. Verify repository call arguments | Unit | P0 | • `rewardRepository.findByOrgIdAndIdIn(1L, List.of(10L))` called exactly once<br>• `rewardRepository.findByOrgIdAndIdIn(2L, List.of(20L))` called exactly once<br>• `rewardRepository.findByOrgIdAndIdIn(1L, List.of(10L, 20L))` **never** called (cross-org contamination)<br>• `rewardRepository.findByOrgIdAndIdIn(2L, List.of(10L, 20L))` **never** called | | - | - | - | - | UT-22 · FR-007 |
| 23 | RewardCatalogueSyncListener UPSERT event with active reward triggers bulkUpsert with correct orgId | **Prerequisites:**<br>1. Same mock setup as test case #22<br>2. Prepare enabled reward: `orgId=1L`, `rewardId=100L`, `isEnabled=true`, `startDate=yesterday`, `endDate=tomorrow`<br>3. `when(rewardRepository.findByOrgIdAndIdIn(1L, [100L])).thenReturn(List.of(activeReward))`<br>4. `ArgumentCaptor<List<ActiveRewardDoc>> docCaptor`<br><br>**Steps:**<br>1. Send batch: `[toJson(orgId=1L, rewardId=100L, UPSERT)]`<br>2. Call `listener.onBatch(messages)`<br>3. `verify(activeRewardRepo).bulkUpsert(docCaptor.capture())` | Unit | P0 | • `activeRewardRepo.bulkUpsert()` called exactly once<br>• `docCaptor.getValue()` contains one `ActiveRewardDoc` with `orgId=1L` and `rewardId=100L`<br>• `catalogueRepo.bulkUpsert()` also called (reward is active) | | - | - | - | - | UT-23 · FR-002 |
| 24 | RewardCatalogueSyncListener UPSERT event with disabled reward triggers delete, not bulkUpsert | **Prerequisites:**<br>1. Same mock setup as test case #22<br>2. Prepare disabled reward: `isEnabled=false`, `orgId=1L`, `rewardId=100L`<br>3. `when(rewardRepository.findByOrgIdAndIdIn(1L, [100L])).thenReturn(List.of(disabledReward))`<br><br>**Steps:**<br>1. Send batch: `[toJson(orgId=1L, rewardId=100L, UPSERT)]`<br>2. Call `listener.onBatch(messages)` | Unit | P0 | • `activeRewardRepo.deleteByOrgIdAndRewardIdIn(1L, [100L])` called exactly once<br>• `activeRewardRepo.bulkUpsert(any())` **never** called for this reward<br>• `catalogueCache.invalidateOrg(1L)` called (cache still invalidated even on removal) | | - | - | - | - | UT-24 · FR-004 |
| 25 | RewardCatalogueSyncListener UPSERT event with expired reward (endDate past) triggers delete | **Prerequisites:**<br>1. Same mock setup<br>2. Prepare expired reward: `isEnabled=true`, `endDate=LocalDateTime.now().minusDays(1)`<br><br>**Steps:**<br>1. Send batch with expired reward<br>2. Call `listener.onBatch(messages)` | Unit | P0 | • `activeRewardRepo.deleteByOrgIdAndRewardIdIn()` called for this reward<br>• `activeRewardRepo.bulkUpsert()` **not** called for expired reward | | - | - | - | - | UT-25 · FR-004 |
| 26 | RewardCatalogueSyncListener UPSERT event with future-start reward (startDate future) triggers delete | **Prerequisites:**<br>1. Same mock setup<br>2. Prepare not-yet-started reward: `isEnabled=true`, `startDate=LocalDateTime.now().plusDays(1)`<br><br>**Steps:**<br>1. Send batch with future-start reward<br>2. Call `listener.onBatch(messages)` | Unit | P0 | • `activeRewardRepo.deleteByOrgIdAndRewardIdIn()` called<br>• `activeRewardRepo.bulkUpsert()` **not** called | | - | - | - | - | UT-26 · FR-004 |
| 27 | RewardCatalogueSyncListener reward not found in MySQL logs WARN and makes no MongoDB writes | **Prerequisites:**<br>1. Same mock setup<br>2. `when(rewardRepository.findByOrgIdAndIdIn(1L, [999L])).thenReturn(Collections.emptyList())`<br><br>**Steps:**<br>1. Send batch: `[toJson(orgId=1L, rewardId=999L, UPSERT)]`<br>2. Call `listener.onBatch(messages)` | Unit | P0 | • No exception thrown from `onBatch()`<br>• `activeRewardRepo.bulkUpsert(any())` **never** called<br>• `activeRewardRepo.deleteByOrgIdAndRewardIdIn(any(), any())` **never** called<br>• WARN log line emitted (verified via captured log appender or `@Spy Logger`) | | - | - | - | - | UT-27 · FR-010 |
| 28 | RewardCatalogueSyncListener re-throws exception from processOrgBatch | **Prerequisites:**<br>1. Same mock setup<br>2. `when(catalogueRepo.bulkUpsert(any())).thenThrow(new RuntimeException("mongo write failed"))`<br>3. Mock valid active reward returned by `rewardRepository`<br><br>**Steps:**<br>1. Call `listener.onBatch(messages)` inside `assertThrows(RuntimeException.class, ...)` | Unit | P0 | • `RuntimeException` is thrown from `onBatch()`<br>• Exception message contains `"mongo write failed"`<br>• RMQ will re-deliver this message for retry | | - | - | - | - | UT-28 · FR-010 |
| 29 | RewardCatalogueSyncListener invalidates catalogue cache exactly once per org after UPSERT | **Prerequisites:**<br>1. Batch: `[{orgId:1, rewardId:10, UPSERT}, {orgId:1, rewardId:11, UPSERT}, {orgId:2, rewardId:20, UPSERT}]`<br>2. Mock rewards returned for each org<br><br>**Steps:**<br>1. Call `listener.onBatch(messages)` | Unit | P0 | • `catalogueCache.invalidateOrg(1L)` called **exactly once** (not twice despite 2 events for org 1)<br>• `catalogueCache.invalidateOrg(2L)` called exactly once<br>• Total invalidation calls = 2 | | - | - | - | - | UT-29 · FR-006 |
| 30 | RewardCatalogueSyncListener filters out events with null orgId | **Prerequisites:**<br>1. Batch includes: `[toJson(orgId=null, rewardId=10L, UPSERT), toJson(orgId=1L, rewardId=20L, UPSERT)]`<br>2. Mock valid reward for orgId=1L<br><br>**Steps:**<br>1. Call `listener.onBatch(messages)` | Unit | P1 | • No exception thrown<br>• `rewardRepository.findByOrgIdAndIdIn(null, any())` **never** called<br>• orgId=1L event processed normally | | - | - | - | - | UT-30 · FR-007 |
| 31 | RewardCatalogueSyncListener filters out events with null rewardId | **Prerequisites:**<br>1. Batch includes event with `rewardId=null`<br><br>**Steps:**<br>1. Call `listener.onBatch(messages)` | Unit | P1 | • No exception thrown<br>• Event with null rewardId silently skipped<br>• No repo calls for the null-rewardId event | | - | - | - | - | UT-31 · FR-007 |
| 32 | RewardCatalogueSyncListener DELETE event triggers delete on both repos | **Prerequisites:**<br>1. Same mock setup<br>2. Batch: `[toJson(orgId=1L, rewardId=100L, operation=DELETE)]`<br><br>**Steps:**<br>1. Call `listener.onBatch(messages)` | Unit | P0 | • `catalogueRepo.deleteByOrgIdAndRewardIdIn(1L, [100L])` called exactly once<br>• `activeRewardRepo.deleteByOrgIdAndRewardIdIn(1L, [100L])` called exactly once<br>• `rewardRepository.findByOrgIdAndIdIn()` **not** called (no MySQL read needed for delete) | | - | - | - | - | UT-32 · FR-010 |
| 33 | RewardCatalogueSyncListener handles duplicate rewardId in batch idempotently | **Prerequisites:**<br>1. Batch: `[{orgId:1, rewardId:100, UPSERT}, {orgId:1, rewardId:100, UPSERT}]` — same reward twice<br>2. Mock `rewardRepository.findByOrgIdAndIdIn(1L, [100L])` → single reward (de-duped query)<br><br>**Steps:**<br>1. Call `listener.onBatch(messages)` | Unit | P1 | • `rewardRepository.findByOrgIdAndIdIn(1L, [100L])` called exactly **once** (de-duplicated rewardIds before MySQL query)<br>• `activeRewardRepo.bulkUpsert()` called once (upsert is idempotent)<br>• No duplicate processing | | - | - | - | - | UT-33 · FR-007 |
| 34 | CatalogueCache.get() returns Optional.empty() when Redis throws exception | **Prerequisites:**<br>1. `CatalogueCache` under test<br>2. `@Mock RedisTemplate<String, String> redisTemplate`<br>3. `@Mock ValueOperations<String, String> valueOps`<br>4. `when(redisTemplate.opsForValue()).thenReturn(valueOps)`<br>5. `when(valueOps.get(any())).thenThrow(new RedisConnectionFailureException("connection refused"))`<br><br>**Steps:**<br>1. Call `Optional<String> result = cache.get(CacheKey.of(1L, "filterHash", 0))` | Unit | P0 | • `result.isPresent()` is `false`<br>• No exception propagated to caller<br>• Exception is swallowed (logged internally) | | - | - | - | - | UT-34 · FR-006 |
| 35 | CatalogueCache.put() silently absorbs Redis exception | **Prerequisites:**<br>1. Same setup as test case #34<br>2. `when(valueOps.set(any(), any(), any(), any())).thenThrow(new RedisConnectionFailureException("timeout"))`<br><br>**Steps:**<br>1. Call `cache.put(key, "json_payload")` | Unit | P0 | • No exception thrown from `put()`<br>• Method returns normally | | - | - | - | - | UT-35 · FR-006 |
| 36 | CatalogueCache.invalidateOrg(orgId=A) deletes only catalogue:A:* keys, leaves catalogue:B:* intact | **Prerequisites:**<br>1. Same Redis mock setup<br>2. `when(redisTemplate.keys("catalogue:1:*")).thenReturn(Set.of("catalogue:1:f1hash:0", "catalogue:1:f2hash:0"))`<br>3. `when(redisTemplate.keys("catalogue:2:*")).thenReturn(Set.of("catalogue:2:f3hash:0"))` — note: this should NOT be called<br>4. `ArgumentCaptor<Collection<String>> deleteCaptor = ArgumentCaptor.forClass(Collection.class)`<br><br>**Steps:**<br>1. Call `cache.invalidateOrg(1L)`<br>2. `verify(redisTemplate).delete(deleteCaptor.capture())` | Unit | P0 | • `redisTemplate.delete()` called exactly once<br>• `deleteCaptor.getValue()` contains exactly `{"catalogue:1:f1hash:0", "catalogue:1:f2hash:0"}`<br>• `"catalogue:2:f3hash:0"` is **not** in the deleted set<br>• `redisTemplate.keys("catalogue:2:*")` **never** called | | - | - | - | - | UT-36 · NFR-002 |
| 37 | CatalogueCache.invalidateOrg() silently absorbs Redis exception during key scan | **Prerequisites:**<br>1. `when(redisTemplate.keys(any())).thenThrow(new RedisConnectionFailureException("timeout"))`<br><br>**Steps:**<br>1. Call `cache.invalidateOrg(1L)` | Unit | P0 | • No exception propagated<br>• `redisTemplate.delete()` **not** called (no keys to delete)<br>• Method returns normally | | - | - | - | - | UT-37 · FR-006 |
| 38 | RewardCatalogueEventPublisher catches RabbitMQ exception and returns normally | **Prerequisites:**<br>1. `RewardCatalogueEventPublisher` under test<br>2. `@Mock RabbitTemplate rabbitTemplate`<br>3. `when(rabbitTemplate.convertAndSend(any(), any(), (Object) any())).thenThrow(new AmqpException("broker unavailable"))`<br><br>**Steps:**<br>1. Call `publisher.publishUpsert(1L, 100L)` | Unit | P0 | • No exception thrown from `publishUpsert()`<br>• Method returns normally<br>• Exception is caught and logged (WARN or ERROR level) | | - | - | - | - | UT-38 · FR-014 |
| 39 | RewardCatalogueEventPublisher.publishUpsert sends JSON with orgId, rewardId, and operation=UPSERT | **Prerequisites:**<br>1. `RewardCatalogueEventPublisher` under test<br>2. `@Mock RabbitTemplate rabbitTemplate`<br>3. `ArgumentCaptor<String> messageCaptor = ArgumentCaptor.forClass(String.class)`<br><br>**Steps:**<br>1. Call `publisher.publishUpsert(1L, 100L)`<br>2. `verify(rabbitTemplate).convertAndSend(any(), any(), messageCaptor.capture())`<br>3. Parse `messageCaptor.getValue()` as JSON | Unit | P0 | • Message body is valid JSON<br>• `jsonNode.get("orgId").asLong()` equals `1L`<br>• `jsonNode.get("rewardId").asLong()` equals `100L`<br>• `jsonNode.get("operation").asText()` equals `"UPSERT"` | | - | - | - | - | UT-39 · FR-002 |
| 40 | RewardCatalogueEventPublisher publishes to correct exchange and routing key | **Prerequisites:**<br>1. Same setup as test case #39<br>2. `ArgumentCaptor<String> exchangeCaptor` and `ArgumentCaptor<String> routingKeyCaptor`<br><br>**Steps:**<br>1. Call `publisher.publishUpsert(1L, 100L)`<br>2. `verify(rabbitTemplate).convertAndSend(exchangeCaptor.capture(), routingKeyCaptor.capture(), any(Object.class))` | Unit | P0 | • `exchangeCaptor.getValue()` equals the value of `RewardCatalogueConstants.REWARDS_CATALOGUE_EXCHANGE`<br>• `routingKeyCaptor.getValue()` equals the value of `RewardCatalogueConstants.REWARDS_CATALOGUE_ROUTING_KEY`<br>• Not an empty string, not null | | - | - | - | - | UT-40 · FR-002 |
| 41 | CatalogueIndexInitializer.ensureIndexes() calls mongoTemplate.indexOps() at least 9 times (8 compound + 1 TTL) | **Prerequisites:**<br>1. `CatalogueIndexInitializer` under test<br>2. `@Mock MongoTemplate mongoTemplate`<br>3. `@Mock IndexOperations indexOps`<br>4. `when(mongoTemplate.indexOps(any(String.class))).thenReturn(indexOps)`<br>5. `when(indexOps.ensureIndex(any())).thenReturn("indexName")`<br>6. `ArgumentCaptor<String> collectionCaptor = ArgumentCaptor.forClass(String.class)`<br><br>**Steps:**<br>1. Call `initializer.ensureIndexes()`<br>2. `verify(mongoTemplate, atLeast(9)).indexOps(collectionCaptor.capture())` | Unit | P0 | • `mongoTemplate.indexOps()` called at least 9 times<br>• Collection names captured include `"active_rewards"` and `"rewards_catalogue"`<br>• `indexOps.ensureIndex()` called at least 9 times total<br>• No exception thrown | | - | - | - | - | UT-41 · FR-015 |
| 42 | CatalogueIndexInitializer creates TTL index on active_rewards.expiresAt with expireAfterSeconds=0 | **Prerequisites:**<br>1. Same setup as test case #41<br>2. `ArgumentCaptor<Index> indexCaptor = ArgumentCaptor.forClass(Index.class)`<br>3. `when(mongoTemplate.indexOps("active_rewards")).thenReturn(indexOps)`<br>4. `verify(indexOps, atLeastOnce()).ensureIndex(indexCaptor.capture())`<br><br>**Steps:**<br>1. Call `initializer.ensureIndexes()`<br>2. Filter captured indexes for the one on field `"expiresAt"` | Unit | P0 | • At least one captured `Index` targets field `"expiresAt"` on collection `"active_rewards"`<br>• That index's expire duration equals `Duration.ZERO` (i.e., `expireAfterSeconds: 0`)<br>• Verified via `index.getIndexOptions().get("expireAfterSeconds")` == `0` | | - | - | - | - | UT-42 · FR-015 |
| 43 | JerseyConfig registers RewardCatalogueResource without error | **Prerequisites:**<br>1. Spring context available or `JerseyConfig` instantiated in unit test<br>2. No mocks required<br><br>**Steps:**<br>1. Instantiate `JerseyConfig jerseyConfig = new JerseyConfig()`<br>2. Call `jerseyConfig.getClasses()` or `jerseyConfig.getSingletons()`<br>3. Check class set | Unit | P0 | • No exception thrown during construction<br>• `RewardCatalogueResource.class` is present in the registered classes set<br>• Existing registered resources are not removed | | - | - | - | - | UT-43 · FR-001 |
| 44 | Create reward via REST triggers RMQ sync; active_rewards and rewards_catalogue both populated within 5s | **Prerequisites:**<br>1. Extends `BaseIntegrationTest` — Testcontainers: MongoDB 5.0, MySQL, RabbitMQ, Redis all started<br>2. `@Autowired MongoTemplate mongoTemplate`<br>3. `@Autowired TestRestTemplate restTemplate`<br>4. `@LocalServerPort int port`<br>5. Auth header: `X-CAP-API-AUTH-ORG-ID: 1`<br>6. `@AfterEach: mongoTemplate.dropCollection("active_rewards"); mongoTemplate.dropCollection("rewards_catalogue")`<br><br>**Steps:**<br>1. POST to `http://localhost:{port}/core/v1/rewards` with valid reward payload (enabled=true, startDate=yesterday, endDate=tomorrow)<br>2. Capture `rewardId` from response body<br>3. Poll `mongoTemplate.findOne(Query.query(Criteria.where("rewardId").is(rewardId)), ActiveRewardDoc.class, "active_rewards")` every 500ms up to 5s<br>4. Poll `rewards_catalogue` similarly | Integration | P0 | • Within 5 seconds: `active_rewards` document with matching `rewardId` and `orgId=1L` is found<br>• Within 5 seconds: `rewards_catalogue` document with matching `rewardId` is found<br>• `active_rewards` doc `orgId` equals `1L`<br>• `rewards_catalogue` doc `imageUrl` is a valid URL path (not a numeric ID) — confirms `getImagePath()` used | | - | - | - | - | IT-01 · FR-002 |
| 45 | Update reward pointsValue via REST; updated value reflected in active_rewards after RMQ sync | **Prerequisites:**<br>1. Same IT base setup as test case #44<br>2. Create reward (as in IT-01) and wait for sync<br>3. Capture `rewardId`<br><br>**Steps:**<br>1. PUT to `/core/v1/rewards/{rewardId}` with updated `pointsValue=750.0`<br>2. Poll `active_rewards` for `rewardId` every 500ms up to 5s<br>3. Check `pointsValue` field | Integration | P0 | • Within 5 seconds: `active_rewards` doc's `pointsValue` equals `750.0`<br>• `rewards_catalogue` doc also updated (if pointsValue is stored there)<br>• Previous `active_rewards` doc is replaced (UPSERT semantics — not duplicate) | | - | - | - | - | IT-02 · FR-003 |
| 46 | Disabling reward via setStatusCategory removes it from active_rewards | **Prerequisites:**<br>1. Same IT base setup<br>2. Create reward and wait for sync<br>3. Confirm `active_rewards` doc exists<br><br>**Steps:**<br>1. Call disable endpoint (e.g., PATCH/PUT to enable/disable reward API with `status=false`)<br>2. Poll absence of `active_rewards` doc for `rewardId` every 500ms up to 5s | Integration | P0 | • Within 5 seconds: `mongoTemplate.findOne(query, "active_rewards")` returns `null` for the `rewardId`<br>• `rewards_catalogue` doc optionally retained (query it to verify delete or keep per design)<br>• `active_rewards` never contains the disabled reward | | - | - | - | - | IT-03 · FR-004 |
| 47 | MongoDB TTL index auto-purges active_rewards doc with past expiresAt within 60 seconds | **Prerequisites:**<br>1. IT base setup — MongoDB Testcontainer with TTL job enabled<br>2. `@BeforeEach: mongoTemplate.dropCollection("active_rewards")`<br><br>**Steps:**<br>1. Insert doc: `ActiveRewardDoc doc = new ActiveRewardDoc(); doc.setExpiresAt(new Date(System.currentTimeMillis() - 1000)); mongoTemplate.save(doc, "active_rewards")`<br>2. Immediately verify doc exists: `mongoTemplate.count(query, "active_rewards") == 1`<br>3. Poll `mongoTemplate.count(new Query(), "active_rewards")` every 5s up to 70 seconds | Integration | P2 | • Within 70 seconds: `mongoTemplate.count(new Query(), "active_rewards")` equals `0`<br>• Note: TTL sweep runs every 60s in MongoDB — may take up to 61s after document reaches expiry | | - | - | - | - | IT-04 · FR-009 · P2 — Run in a separate slow test category |
| 48 | GET catalogue with no filters returns all active rewards for org | **Prerequisites:**<br>1. IT base setup<br>2. `@BeforeEach:` insert 3 `ActiveRewardDoc` docs with `orgId=1L`, distinct `rewardId`s (rewardId=1,2,3), `expiresAt=tomorrow`, and 3 matching `RewardCatalogueDoc` docs<br>3. `@AfterEach: dropCollection("active_rewards"); dropCollection("rewards_catalogue")`<br><br>**Steps:**<br>1. GET `http://localhost:{port}/core/v1/rewards/catalogue` with header `X-CAP-API-AUTH-ORG-ID: 1`<br>2. Parse response body as `CatalogueListResponse` | Integration | P0 | • HTTP 200<br>• `response.getItems().size()` equals `3`<br>• `response.getTotal()` equals `3`<br>• Each item has `rewardId`, `name`, `imageUrl`, `pointsValue` populated | | - | - | - | - | IT-05 · FR-001 |
| 49 | GET catalogue with groupIds filter returns only rewards matching specified groupId | **Prerequisites:**<br>1. IT base setup<br>2. Insert `ActiveRewardDoc` with `orgId=1L, rewardId=1L, groupIds=[10L, 20L]`<br>3. Insert `ActiveRewardDoc` with `orgId=1L, rewardId=2L, groupIds=[30L]`<br>4. Insert matching `RewardCatalogueDoc` for both<br><br>**Steps:**<br>1. GET `/core/v1/rewards/catalogue?groupIds=10` with header `X-CAP-API-AUTH-ORG-ID: 1`<br>2. Parse response | Integration | P0 | • HTTP 200<br>• `response.getItems().size()` equals `1`<br>• Returned item has `rewardId=1L`<br>• Item with `rewardId=2L` (groupId=30) not present | | - | - | - | - | IT-06 · FR-001 |
| 50 | GET catalogue with labelIds filter returns only rewards matching specified labelId | **Prerequisites:**<br>1. IT base setup<br>2. Insert 2 `ActiveRewardDoc` docs: one with `labelIds=[5L]`, one with `labelIds=[6L]`<br><br>**Steps:**<br>1. GET `/core/v1/rewards/catalogue?labelIds=5` with header `X-CAP-API-AUTH-ORG-ID: 1` | Integration | P0 | • HTTP 200<br>• Only reward with `labelIds` containing `5L` returned<br>• Reward with `labelIds=[6L]` not included | | - | - | - | - | IT-07 · FR-001 |
| 51 | GET catalogue with pointsMin and pointsMax range filter returns only rewards in range | **Prerequisites:**<br>1. IT base setup<br>2. Insert 3 `ActiveRewardDoc`: `rewardId=1, pointsValue=50.0`, `rewardId=2, pointsValue=200.0`, `rewardId=3, pointsValue=600.0`<br><br>**Steps:**<br>1. GET `/core/v1/rewards/catalogue?pointsMin=100&pointsMax=500` | Integration | P0 | • HTTP 200<br>• `response.getItems().size()` equals `1`<br>• Returned item has `rewardId=2L` (pointsValue=200)<br>• Items with pointsValue=50 and pointsValue=600 not present | | - | - | - | - | IT-08 · FR-001 |
| 52 | GET catalogue with pointsExact filter returns exact match only | **Prerequisites:**<br>1. IT base setup<br>2. Insert 3 docs at `pointsValue=200, 300, 400`<br><br>**Steps:**<br>1. GET `/core/v1/rewards/catalogue?pointsExact=300` | Integration | P1 | • HTTP 200<br>• `response.getItems().size()` equals `1`<br>• Returned item has `pointsValue=300.0` | | - | - | - | - | IT-09 · FR-001 |
| 53 | GET catalogue with conflicting pointsExact + pointsMin returns 400 | **Prerequisites:**<br>1. IT base setup — no seed data needed<br><br>**Steps:**<br>1. GET `/core/v1/rewards/catalogue?pointsExact=300&pointsMin=100` with valid auth header | Integration | P0 | • HTTP 400<br>• Response body contains error indicating mutually exclusive parameters<br>• No MongoDB query executed | | - | - | - | - | IT-10 · FR-011 |
| 54 | GET catalogue for org with no rewards returns empty response | **Prerequisites:**<br>1. IT base setup — no seed data for org 1<br>2. Ensure `active_rewards` collection empty for orgId=1<br><br>**Steps:**<br>1. GET `/core/v1/rewards/catalogue` with `X-CAP-API-AUTH-ORG-ID: 1` | Integration | P0 | • HTTP 200<br>• `response.getItems()` is empty array `[]`<br>• `response.getTotal()` equals `0`<br>• `response.getNextCursor()` is `null` | | - | - | - | - | IT-11 · FR-014 |
| 55 | Cursor pagination produces no duplicates and no skips across two pages | **Prerequisites:**<br>1. IT base setup<br>2. Insert 7 `ActiveRewardDoc` docs with distinct `rewardId`s and `pointsValue`s (e.g., 100, 200, 300, 400, 500, 600, 700)<br>3. Default limit = 5<br><br>**Steps:**<br>1. GET `/core/v1/rewards/catalogue?limit=5` — capture `nextCursor` from response<br>2. Assert page 1 result<br>3. GET `/core/v1/rewards/catalogue?limit=5&cursor={nextCursor}` — capture page 2 result<br>4. Merge both result sets and verify completeness | Integration | P0 | • Page 1: `response.getItems().size()` equals `5`; `response.getNextCursor()` not null<br>• Page 2: `response.getItems().size()` equals `2`; `response.getNextCursor()` is null<br>• Total unique `rewardId`s across page 1 + page 2 = 7 (no duplicates, no skips)<br>• Page 2 items have higher `pointsValue` than last item of page 1 (sort order preserved) | | - | - | - | - | IT-12 · FR-005 |
| 56 | Cursor remains valid for page 2 after Redis cache invalidation between pages | **Prerequisites:**<br>1. Same setup as test case #55<br>2. Redis template available: `@Autowired RedisTemplate<String, String> redisTemplate`<br><br>**Steps:**<br>1. GET page 1 — capture `nextCursor`<br>2. Manually invalidate Redis: `redisTemplate.delete(redisTemplate.keys("catalogue:1:*"))`<br>3. GET page 2 with `cursor={nextCursor}` | Integration | P1 | • Page 2 returns correct 2 items even after cache bust<br>• Cursor is decoded from base64 on every request (stateless)<br>• No error, no duplicate items from page 1 | | - | - | - | - | IT-13 · FR-005 |
| 57 | Second consecutive GET request within 30s results in Redis cache HIT | **Prerequisites:**<br>1. IT base setup — seed 2 `active_rewards` + `rewards_catalogue` docs<br>2. `@Autowired RedisTemplate<String, String> redisTemplate`<br><br>**Steps:**<br>1. GET `/core/v1/rewards/catalogue` (first call — cache miss, populates Redis)<br>2. Wait 100ms<br>3. GET `/core/v1/rewards/catalogue` (second call — should hit cache)<br>4. Check Redis: `redisTemplate.keys("catalogue:1:*")` | Integration | P0 | • Both requests return HTTP 200<br>• Both return same `items` and `total`<br>• After second GET: `redisTemplate.keys("catalogue:1:*")` returns a non-empty set<br>• At least one key exists matching `catalogue:1:` prefix | | - | - | - | - | IT-14 · FR-006 |
| 58 | Redis cache invalidated after reward mutation; subsequent GET re-queries MongoDB | **Prerequisites:**<br>1. IT base setup — seed docs for org 1<br>2. First GET to populate Redis cache<br>3. Verify Redis key exists<br><br>**Steps:**<br>1. GET catalogue — cache populated<br>2. Verify `redisTemplate.keys("catalogue:1:*")` non-empty<br>3. Trigger reward disable via REST (or directly via `RewardFacade.setStatusCategory()`)<br>4. Wait up to 5s for RMQ sync + cache invalidation<br>5. Assert `redisTemplate.keys("catalogue:1:*")` | Integration | P0 | • Before mutation: Redis key present<br>• After mutation + sync: `redisTemplate.keys("catalogue:1:*")` returns empty set (all org keys deleted)<br>• Subsequent GET after invalidation hits MongoDB (cache miss) and returns updated result set | | - | - | - | - | IT-15 · FR-006 |
| 59 | Transaction rollback on reward create does NOT trigger afterCommit; active_rewards not populated | **Prerequisites:**<br>1. IT base setup<br>2. Strategy: create reward with a valid payload but instrument the service to throw after MySQL commit but before afterCommit fires — OR use a reward payload that triggers DB constraint violation<br>3. Alternative: use `TransactionTestUtils` to force rollback on the test transaction<br><br>**Steps:**<br>1. Attempt reward create that results in transaction rollback<br>2. Wait 3s for any async events that should NOT arrive<br>3. Query `active_rewards` for any doc with the attempted rewardId | Integration | P0 | • No `active_rewards` doc found for the attempted rewardId after 3s<br>• Reward also absent from MySQL (confirms rollback)<br>• RMQ queue does not receive event (verifiable via RabbitMQ management API on test container) | | - | - | - | - | RG-01 · FR-013 |
| 60 | Reward create API returns HTTP 200 even when RabbitMQ is unavailable | **Prerequisites:**<br>1. IT base setup<br>2. Stop/pause RabbitMQ Testcontainer: `rabbitMQContainer.stop()` in `@BeforeEach`<br>3. Note: `RewardCatalogueEventPublisher.publishUpsert()` must swallow the exception<br><br>**Steps:**<br>1. POST to `/core/v1/rewards` with valid payload<br>2. Capture HTTP response status<br>3. Query MySQL for the created reward | Integration | P0 | • HTTP response status equals `200` (reward write succeeds regardless of RMQ state)<br>• Reward exists in MySQL (confirmed via REST GET or direct DB query)<br>• No `active_rewards` doc for this rewardId (expected — sync failed)<br>• No exception propagated to caller | | - | - | - | - | RG-02 · FR-014 |
| 61 | Reward update API response time is unaffected by afterCommit registration | **Prerequisites:**<br>1. IT base setup<br>2. Create reward and sync<br>3. `long start; long elapsed`<br><br>**Steps:**<br>1. `start = System.currentTimeMillis()`<br>2. PUT to `/core/v1/rewards/{rewardId}` with minor update<br>3. `elapsed = System.currentTimeMillis() - start`<br>4. Assert elapsed time | Integration | P1 | • HTTP 200 returned<br>• `elapsed < 500` (ms) — afterCommit registration itself must not add blocking latency to the API response<br>• Response body matches expected updated reward data | | - | - | - | - | RG-03 · FR-003 |
| 62 | Cross-tenant GET: Org-A rewards not visible to Org-B query | **Prerequisites:**<br>1. `CatalogueTenantIsolationIT` extends `BaseIntegrationTest`<br>2. Insert 3 `ActiveRewardDoc` docs with `orgId=1L` (Org-A)<br>3. No docs for `orgId=2L` (Org-B)<br>4. `@AfterEach: dropCollection("active_rewards"); dropCollection("rewards_catalogue")`<br><br>**Steps:**<br>1. GET `/core/v1/rewards/catalogue` with header `X-CAP-API-AUTH-ORG-ID: 2`<br>2. Parse response | Tenant Isolation | P0 | • HTTP 200<br>• `response.getItems()` is empty (`size() == 0`)<br>• `response.getTotal()` equals `0`<br>• Org-A docs are NOT in the response — `orgId` filter in MongoDB query is enforced | | - | - | - | - | TI-01 · NFR-002 |
| 63 | Cross-tenant cache invalidation: Org-A mutation does not delete Org-B Redis keys | **Prerequisites:**<br>1. IT setup with Redis<br>2. Manually set Org-A cache key: `redisTemplate.opsForValue().set("catalogue:1:hash1:0", "data", 30, TimeUnit.SECONDS)`<br>3. Manually set Org-B cache key: `redisTemplate.opsForValue().set("catalogue:2:hash2:0", "data", 30, TimeUnit.SECONDS)`<br><br>**Steps:**<br>1. Trigger Org-A mutation (disable a reward for orgId=1) via REST or direct call<br>2. Wait for async invalidation (2s)<br>3. Check both org cache keys | Tenant Isolation | P0 | • `redisTemplate.hasKey("catalogue:1:hash1:0")` returns `false` (Org-A key deleted)<br>• `redisTemplate.hasKey("catalogue:2:hash2:0")` returns `true` (Org-B key NOT deleted) | | - | - | - | - | TI-02 · NFR-002 |
| 64 | Cross-tenant batch listener: MySQL IN queries scoped per org, never cross-org | **Prerequisites:**<br>1. Unit test — covered by test case #22 (`RewardCatalogueSyncListenerTest`)<br>2. Send mixed-org batch `[{orgId:1, rewardId:10}, {orgId:2, rewardId:20}]`<br><br>**Steps:**<br>1. Verify repository calls via Mockito (already covered in UT-22) | Tenant Isolation | P0 | • `findByOrgIdAndIdIn(1L, [10L])` — org 1 queried with only org 1's reward IDs<br>• `findByOrgIdAndIdIn(2L, [20L])` — org 2 queried with only org 2's reward IDs<br>• Cross-org call `findByOrgIdAndIdIn(1L, [10L, 20L])` never happens<br>*Note: Covered by UT-22 (test case #22)* | Tenant Isolation | P0 | — | | - | - | - | - | TI-03 · NFR-002 (see UT-22) |
| 65 | Cross-tenant index isolation: Org-B query returns 0 results when only Org-A docs exist | **Prerequisites:**<br>1. IT setup (subset of TI-01)<br>2. Seed only Org-A (`orgId=1L`) docs in `active_rewards`<br><br>**Steps:**<br>1. Directly query MongoDB: `mongoTemplate.find(Query.query(Criteria.where("orgId").is(2L)), ActiveRewardDoc.class, "active_rewards")`<br>2. Assert result size | Tenant Isolation | P0 | • Result list is empty<br>• `orgId` field in MongoDB index acts as partition — Org-B documents physically absent<br>*Note: Covered by IT-level test TI-01 (test case #62)* | | - | - | - | - | TI-04 · NFR-002 (see TI-01) |
| 66 | Redis key isolation: invalidateOrg(A) deletes only catalogue:A:* prefix, not catalogue:B:* | **Prerequisites:**<br>1. Unit test — covered by test case #36 (`CatalogueCacheTest`)<br>2. Mock `redisTemplate.keys("catalogue:1:*")` returns Org-A keys<br>3. Verify `delete()` called with only Org-A key set<br><br>**Steps:**<br>1. Call `cache.invalidateOrg(1L)` (already covered in UT-36) | Tenant Isolation | P0 | • Only `catalogue:1:*` keys deleted<br>• `catalogue:2:*` keys untouched<br>*Note: Covered by UT-36 (test case #36)* | | - | - | - | - | TI-05 · NFR-002 (see UT-36) |

---

## Pre-Implementation Checklist (from tech-detail-reviewed.md — verify before writing any class)

| # | Item | Status |
|---|------|--------|
| B1 | `@RmqMessageTracker` removed from `onBatch()`. Manual MDC setup (3 keys: `X_CAP_API_AUTH_ORG_ID`, `REQUEST_ID_MDC`, `INTERNAL_REQUEST_ID_MDC`) + `MetricsService` tracking added in `processOrgBatch()` | |
| B2 | `CatalogueIndexInitializer.ensureIndexes()` creates ALL 8 compound indexes explicitly via `mongoTemplate.indexOps().ensureIndex()` — not relying on `@CompoundIndexes` annotations | |
| A1 | `buildCatalogueDoc()` uses `details.getImagePath()` not `details.getImageId()` | |
| A2 | Method modified in `RewardFacade` is `setStatusCategory()` at line 1636 — not `updateStatus()` | |
| A3 | `processOrgBatch()` sets `REQUEST_ID_MDC` and `INTERNAL_REQUEST_ID_MDC` in addition to `X_CAP_API_AUTH_ORG_ID`; removes all 3 in `finally` block | |

---

## Summary

| Field | Value |
|-------|-------|
| Total test cases | 66 |
| Passed | |
| Failed | |
| Not tested | |
| Nightly environment | crm-nightly-new |
| Test promotion ID | |
| Test date | |
