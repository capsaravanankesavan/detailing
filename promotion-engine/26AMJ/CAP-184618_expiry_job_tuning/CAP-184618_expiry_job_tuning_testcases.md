# Expiry Date Change Job — MongoDB Load Reduction

## Metadata

| Field | Value |
|-------|-------|
| Ticket | [CAP-184618](https://capillarytech.atlassian.net/browse/CAP-184618) |
| Description | Replace range-based MongoDB updateMulti with $in point lookups; Redis INCR/DECR semaphore capping cross-pod concurrency at maxGlobalConcurrentJobs=3; WAITING BatchStatus for throttle-retry resilience; SemaphoreRejectedException + RMQMessageTrackerAspect filter (Fix-A); StatefulRetryOperationsInterceptor MessageRecoverer on dedicated container factory (Fix-B) |
| Module | promotion-engine |
| Developer / Owner | |
| Reviewer | |
| Date | 2026-05-08 |
| Pull request | |
| Releases detailing | |
| Preconditions | |
| Security Impact | NA |

---

## Test Cases

| # | Test Case | Test Steps | Type | Priority | Expected Result | Actual Result | Dev | Nightly | Staging | Prod | Comments |
|---|-----------|------------|------|----------|-----------------|---------------|-----|---------|---------|------|----------|
| 1 | getAggregation() pushes all _id values into list; no $sort stage | **Prerequisites:**<br>1. `@RunWith(MockitoJUnitRunner.class)`, `@InjectMocks CustomerIssuedCollectionStaticExpiryDateChangeDao dao`, `@Mock MongoOperations mongoOperation`<br>2. Build stub job: `orgId=100`, `promotionId="500"`, `startId=new ObjectId("000000000000000000000000")`<br>3. Stub `mongoOperation.aggregate(any(), anyString(), any())` → empty `AggregationResults<BasicDBObject>`<br>4. Declare `ArgumentCaptor<TypedAggregation> captor`<br><br>**Steps:**<br>1. Call `dao.updateValidTill(stubJob, promotionMeta)` (invokes `getAggregation()` internally)<br>2. Capture `TypedAggregation` from `mongoOperation.aggregate()` via captor<br>3. Inspect pipeline stages list | Unit | P0 | • Pipeline `GroupOperation` field `"ids"` uses `push("_id")` — NOT `last("_id")`<br>• Pipeline contains NO `SortOperation`<br>• `MatchOperation` includes `orgId=100` AND `promotionId="500"` | | - | - | - | - | UT-01 · FR-001 · CustomerIssuedCollectionStaticExpiryDateChangeDaoTest |
| 2 | updateValidTill() loop extracts batchIds and advances startId to last ID | **Prerequisites:**<br>1. Class setup as #1; build stub job<br>2. Declare `ObjectId id1, id2, id3`; build `BasicDBList` with all three; wrap in `AggregationResults` under key `BATCH_IDS`<br>3. Stub `mongoOperation.aggregate()`: first call → 3-ID result; second call → empty<br>4. Spy on `dao` to capture `updateValidTillDate()` args<br><br>**Steps:**<br>1. Call `dao.updateValidTill(stubJob, promotionMeta)`<br>2. Verify `updateValidTillDate()` call count and arguments<br>3. Inspect `stubJob.getStartId()` after the loop | Unit | P0 | • `updateValidTillDate()` called exactly once with `List<ObjectId>` containing `[id1, id2, id3]`<br>• `stubJob.getStartId() == id3` (advanced to last ID in batch)<br>• `mongoOperation.aggregate()` called twice (once with data, once empty to exit loop) | | - | - | - | - | UT-02 · FR-001 |
| 3 | updateValidTillDate() uses $in criteria with orgId filter — no range predicate | **Prerequisites:**<br>1. Class setup as #1; stub job with `orgId=100`<br>2. Build `List<ObjectId> batchIds = Arrays.asList(id1, id2, id3)`<br>3. Stub `mongoOperation.updateMulti(any(), any(), any(), anyString())` → `UpdateResult.acknowledged(3, 3L, null)`<br>4. Declare `ArgumentCaptor<Query> queryCaptor`<br><br>**Steps:**<br>1. Call `dao.updateValidTillDate(stubJob, batchIds, promotionMeta)`<br>2. Capture `Query` arg from `updateMulti()` via captor<br>3. Inspect `queryCaptor.getValue().getQueryObject()` | Unit | P0 | • `queryObject` contains `_id` with sub-key `$in` — NOT `$gt` or `$lte`<br>• `queryObject` contains `orgId: 100`<br>• No `$gt` / `$lte` / `$lt` operators anywhere in criteria | | - | - | - | - | UT-04 · FR-001, NFR-002 |
| 4 | EARNED-dynamic path: updateValidTillDate() uses $in + AggregationUpdate (not plain Update) | **Prerequisites:**<br>1. `@RunWith(MockitoJUnitRunner.class)`, `@InjectMocks CustomerEarnedCollectionDynamicExpiryDateChangeDao dao`, `@Mock MongoOperations mongoOperation`<br>2. Stub job: `orgId=200`; build `List<ObjectId> batchIds = Arrays.asList(id1, id2)`; build stub `PromotionMeta`<br>3. Stub `mongoOperation.updateMulti(any(), any(), any(), anyString())` → acknowledged result<br>4. Declare `ArgumentCaptor<Query> qCaptor`, `ArgumentCaptor<UpdateDefinition> uCaptor`<br><br>**Steps:**<br>1. Call `dao.updateValidTillDate(stubJob, batchIds, promotionMeta)`<br>2. Capture `Query` and `UpdateDefinition` from `updateMulti()` | Unit | P0 | • `qCaptor.getValue()` criteria contains `$in` under `_id`<br>• `qCaptor.getValue()` criteria contains `orgId: 200`<br>• `uCaptor.getValue() instanceof AggregationUpdate` (NOT plain `Update`) | | - | - | - | - | UT-05 · FR-002 · CustomerEarnedCollectionDynamicExpiryDateChangeDaoTest |
| 5 | Existing shouldUpdateValidTillDate test passes after signature change to List<ObjectId> | **Prerequisites:**<br>1. Open existing `shouldUpdateValidTillDate` test in `CustomerEarnedCollectionDynamicExpiryDateChangeDaoTest`<br>2. Replace `ObjectId endId` parameter with `List<ObjectId> batchIds = Collections.singletonList(existingId)`<br>3. Leave all existing stubs and verify calls unchanged<br><br>**Steps:**<br>1. Run the modified test method | Unit | P0 | • Test passes (green)<br>• All existing `verify()` calls on `mongoOperation.updateMulti()` pass unchanged<br>• No assertion changes required | | - | - | - | - | UT-06 · FR-002 · Regression |
| 6 | tryAcquireSemaphore() returns true when counter is within limit | **Prerequisites:**<br>1. `@RunWith(MockitoJUnitRunner.class)`, `@InjectMocks ExpiryDateChangeJobFacade facade`, `@Mock RedisTemplate<String,String> redisTemplate`, `@Mock ValueOperations<String,String> valueOperations`<br>2. `@Before`: `when(redisTemplate.opsForValue()).thenReturn(valueOperations)`, inject `maxGlobalConcurrentJobs=3`, inject `semaphoreKey="promotion_engine:expiry_job:running_count"`<br>3. Stub `valueOperations.increment("promotion_engine:expiry_job:running_count")` → `2L`<br><br>**Steps:**<br>1. Call `facade.tryAcquireSemaphore("job-1", 100L)` | Unit | P0 | • Returns `true`<br>• `valueOperations.decrement(...)` never called (no over-limit rollback) | | - | - | - | - | UT-07 · FR-003 · ExpiryDateChangeJobFacadeTest |
| 7 | tryAcquireSemaphore() returns false and decrements when counter exceeds limit | **Prerequisites:**<br>1. Class setup as #6<br>2. Stub `valueOperations.increment(...)` → `4L` (over limit of 3)<br><br>**Steps:**<br>1. Call `facade.tryAcquireSemaphore("job-1", 100L)`<br>2. Verify decrement call | Unit | P0 | • Returns `false`<br>• `valueOperations.decrement("promotion_engine:expiry_job:running_count")` called exactly once (counter rolled back) | | - | - | - | - | UT-08 · FR-003 · Tenant Isolation: TI-02 (global semaphore applies to all orgs) |
| 8 | tryAcquireSemaphore() is fail-open — returns true when Redis throws | **Prerequisites:**<br>1. Class setup as #6<br>2. Stub `valueOperations.increment(...)` throws `new RedisConnectionFailureException("cluster unreachable")`<br><br>**Steps:**<br>1. Call `facade.tryAcquireSemaphore("job-1", 100L)` | Unit | P0 | • Returns `true` (fail-open — Redis unavailability must not block jobs)<br>• No exception propagated to caller<br>• `decrement()` never called | | - | - | - | - | UT-09 · FR-004 |
| 9 | On semaphore rejection: job transitions to WAITING, SemaphoreRejectedException thrown with correct message | **Prerequisites:**<br>1. Class setup as #6; build `ExpiryDateChangeJob` with `status=OPEN`, `jobId="job-abc"`, `orgId=100L`<br>2. Stub `expiryDateChangeJobService.findById("job-abc")` → the OPEN job<br>3. Stub `expiryDateChangeJobService.updateExpiryDateChangeJobStatus(any(), eq(BatchStatus.WAITING))` → updated job<br>4. Create spy: `ExpiryDateChangeJobFacade spy = Mockito.spy(facade)`; `doReturn(false).when(spy).tryAcquireSemaphore(any(), anyLong())`<br><br>**Steps:**<br>1. `SemaphoreRejectedException ex = assertThrows(SemaphoreRejectedException.class, () -> spy.changeExpiryForPromotion("job-abc"))`<br>2. Verify `updateExpiryDateChangeJobStatus` call<br>3. Inspect exception message | Unit | P0 | • `SemaphoreRejectedException` thrown (exact type — NOT merely `RuntimeException`)<br>• `expiryDateChangeJobService.updateExpiryDateChangeJobStatus(job, BatchStatus.WAITING)` called before exception propagates<br>• `ex.getMessage()` contains `"concurrency limit"` | | - | - | - | - | UT-12+W1+W2 · FR-006, FR-010, FR-014 · Merged from UT-12, UT-W1, UT-W2 |
| 10 | releaseSemaphore() called in finally even when processing block throws | **Prerequisites:**<br>1. Class setup as #6; build OPEN job; stub `findById()` → the job<br>2. Create spy; `doReturn(true).when(spy).tryAcquireSemaphore(any(), anyLong())`<br>3. Stub internal processing step to throw `new RuntimeException("processing error")`<br><br>**Steps:**<br>1. `assertThrows(RuntimeException.class, () -> spy.changeExpiryForPromotion("job-abc"))`<br>2. Verify `releaseSemaphore` call | Unit | P0 | • `RuntimeException` propagates (processing failure not swallowed)<br>• `spy.releaseSemaphore(any(), anyLong())` called exactly once (finally block guarantee) | | - | - | - | - | UT-13 · FR-005 · Regression: RG-06 |
| 11 | reconcileSemaphoreCounter() sets Redis key to actual running count | **Prerequisites:**<br>1. Class setup as #6<br>2. Stub `valueOperations.set(anyString(), anyString())`<br><br>**Steps:**<br>1. Call `facade.reconcileSemaphoreCounter(2L)`<br>2. Capture args passed to `valueOperations.set()` | Unit | P0 | • `valueOperations.set("promotion_engine:expiry_job:running_count", "2")` called exactly once<br>• Key is exactly `"promotion_engine:expiry_job:running_count"` (no prefix variation) | | - | - | - | - | UT-14 · FR-007 |
| 12 | reconcileSemaphoreCounter() does not throw when Redis is down | **Prerequisites:**<br>1. Class setup as #6<br>2. Stub `valueOperations.set(any(), any())` throws `new RedisConnectionFailureException("down")`<br><br>**Steps:**<br>1. Call `facade.reconcileSemaphoreCounter(1L)` | Unit | P0 | • No exception propagated<br>• Method completes normally | | - | - | - | - | UT-16 · FR-007 |
| 13 | Reconciliation called post-loop with sum of all shards — not inside the shard loop | **Prerequisites:**<br>1. `@RunWith(MockitoJUnitRunner.class)`, `@InjectMocks ExpiryDateChangeHelper helper`, `@Mock ExpiryDateChangeJobFacade facade`, `@Mock ExpiryDateChangeJobDao expiryDateChangeJobDao`, `@Mock ExpiryDateChangeJobService expiryDateChangeJobService`<br>2. `@Before`: inject 2 shard keys: `ReflectionTestUtils.setField(helper, "shardKeys", Arrays.asList("shard-1", "shard-2"))`<br>3. Stub `expiryDateChangeJobDao.countByStatus(BatchStatus.RUNNING)` → `1L` (per call)<br>4. Declare `InOrder inOrder = inOrder(expiryDateChangeJobService, facade)`<br><br>**Steps:**<br>1. Call `helper.updateExpiryDateChangeJobs()`<br>2. Verify call ordering and reconcile argument via inOrder | Unit | P0 | • `expiryDateChangeJobService.markAllJobAsExpiredIfNotRunning()` called exactly 2 times (once per shard, inside loop)<br>• `facade.reconcileSemaphoreCounter(2L)` called exactly once (post-loop, sum of both shards)<br>• inOrder confirms: all `markAllJobAsExpiredIfNotRunning()` calls happen BEFORE `reconcileSemaphoreCounter()` | | - | - | - | - | UT-17 · FR-007, UC-4 · ExpiryDateChangeHelperTest |
| 14 | expiryDateChangeContainerFactory: configurer.configure() is the first call before any set* | **Prerequisites:**<br>1. `@RunWith(MockitoJUnitRunner.class)`, `@InjectMocks SpringAmqpConfig config`, `@Mock ConnectionFactory connectionFactory`, `@Mock RabbitListenerContainerFactoryConfigurer configurer`, `@Mock MetricsService metricsService`<br>2. `@Before`: inject retry props via `ReflectionTestUtils.setField(config, "retryMaxAttempts", 10)` (+ `retryInitialInterval`, `retryMultiplier`, `retryMaxInterval`); inject `metricsService`<br>3. Declare `InOrder inOrder = inOrder(configurer)`<br><br>**Steps:**<br>1. Call `config.expiryDateChangeContainerFactory(connectionFactory, configurer)`<br>2. Verify call ordering via inOrder | Unit | P0 | • `configurer.configure(any(SimpleRabbitListenerContainerFactory.class), eq(connectionFactory))` is the FIRST operation<br>• `configure()` is called before any `setConcurrentConsumers()` or `setAdviceChain()` call<br>• Returned factory is not null | | - | - | - | - | UT-19 · FR-008, NFR-005 · SpringAmqpConfigTest |
| 15 | tryAcquireSemaphore() clamps maxGlobalConcurrentJobs=0 to effectiveMax=1; returns true | **Prerequisites:**<br>1. Class setup as #6<br>2. `ReflectionTestUtils.setField(facade, "maxGlobalConcurrentJobs", 0)`<br>3. Stub `valueOperations.increment(...)` → `1L`<br><br>**Steps:**<br>1. Call `facade.tryAcquireSemaphore("job-1", 100L)` | Unit | P0 | • Returns `true` (clamped effectiveMax=1; counter 1 ≤ 1 → allowed — pod not deadlocked)<br>• `decrement()` NOT called<br>• No exception thrown | | - | - | - | - | UT-EC4 · Risk-8 (zero-config deadlock guard) |
| 16 | tryAcquireSemaphore() logs WARN when maxGlobalConcurrentJobs=0 (ops alert) | **Prerequisites:**<br>1. Class setup as #6; set `maxGlobalConcurrentJobs=0`<br>2. Stub `valueOperations.increment(...)` → `1L`<br>3. Attach `ListAppender<ILoggingEvent>` to `ExpiryDateChangeJobFacade` logger<br><br>**Steps:**<br>1. Call `facade.tryAcquireSemaphore("job-1", 100L)`<br>2. Inspect captured log events | Unit | P0 | • At least one WARN-level log event emitted<br>• WARN message contains the misconfigured value (`"0"`) and indicates clamping (e.g., contains `"invalid"` or `"clamped"`)<br>• Enables ops team to detect and correct misconfiguration via logs | | - | - | - | - | UT-EC4b · Risk-8 |
| 17 | shouldProceed() returns true for WAITING; false for COMPLETED, EXPIRED, STOPPED | **Prerequisites:**<br>1. Class setup as #6; build four `ExpiryDateChangeJob` instances — one per status: WAITING, COMPLETED, EXPIRED, STOPPED<br>2. Stub `expiryDateChangeJobService.findById(...)` → the respective job per invocation<br>3. Create spy; `doReturn(true).when(spy).tryAcquireSemaphore(any(), anyLong())`<br><br>**Steps:**<br>1. WAITING: call `spy.changeExpiryForPromotion(jobId)`; verify `tryAcquireSemaphore` invoked (shouldProceed=true, processing attempted)<br>2. COMPLETED: call `spy.changeExpiryForPromotion(jobId)`; verify `tryAcquireSemaphore` NEVER invoked (early return)<br>3. EXPIRED: same as COMPLETED<br>4. STOPPED: same as COMPLETED | Unit | P0 | • WAITING → `tryAcquireSemaphore` called (shouldProceed = true)<br>• COMPLETED, EXPIRED, STOPPED → `tryAcquireSemaphore` NEVER called (shouldProceed = false)<br>• Adding WAITING does not open a hole for any terminal status | | - | - | - | - | UT-W3 · FR-011 · Regression: RG-05 |
| 18 | isJobRunningForPromotion() returns true when a WAITING job exists for same org+promotion+type | **Prerequisites:**<br>1. `@RunWith(MockitoJUnitRunner.class)`, `@InjectMocks ExpiryDateChangeJobService service`, `@Mock ExpiryDateChangeJobDao expiryDateChangeJobDao`<br>2. Build `ExpiryDateChangeJob` with `status=WAITING`, `orgId=100L`, `promotionId="promo-1"`<br>3. Stub `expiryDateChangeJobDao.findByOrgIdAndPromotionIdAndStatusInAndJobType(eq(100L), eq("promo-1"), argThat(list -> list.contains(BatchStatus.WAITING)), any())` → the WAITING job<br>4. Declare `ArgumentCaptor<List<BatchStatus>> statusCaptor`<br><br>**Steps:**<br>1. Call `service.isJobRunningForPromotion(100L, "promo-1", ExpiryDateJobType.ISSUED)`<br>2. Capture statusList arg passed to DAO | Unit | P0 | • Returns `true` (WAITING job found → duplicate creation blocked)<br>• `statusList` passed to DAO CONTAINS `BatchStatus.WAITING` (explicit guard confirmed) | | - | - | - | - | UT-W4 · FR-012, UC-11 · ExpiryDateChangeJobServiceTest |
| 19 | WAITING job older than 2h is expired by markAllJobAsExpiredIfNotRunning() | **Prerequisites:**<br>1. Class setup as #18<br>2. Build WAITING job with `attribution.lastUpdatedOn = Instant.now() minus 3 hours`<br>3. Stub `dao.findByStatusIn(argThat(list -> list.contains(OPEN) \|\| list.contains(RUNNING)))` → empty list<br>4. Stub `dao.findByStatusIn(argThat(list -> list.contains(WAITING) && !list.contains(OPEN)))` → `[waitingJob]`<br>5. Declare `ArgumentCaptor<ExpiryDateChangeJob> saveCaptor`<br><br>**Steps:**<br>1. Call `service.markAllJobAsExpiredIfNotRunning()`<br>2. Capture arg passed to `expiryDateChangeJobDao.save()` | Unit | P0 | • `dao.save()` called exactly once<br>• `saveCaptor.getValue().getStatus() == BatchStatus.EXPIRED`<br>• `saveCaptor.getValue().getAttribution().getLastUpdatedOn()` is AFTER the original 3h-ago timestamp | | - | - | - | - | UT-W5 · FR-013, UC-10 |
| 20 | WAITING job within 2h is NOT expired (active retry window protection) | **Prerequisites:**<br>1. Class setup as #18<br>2. Build WAITING job with `attribution.lastUpdatedOn = Instant.now() minus 30 minutes`<br>3. Stub `dao.findByStatusIn(WAITING)` → `[activeRetryJob]`; stub `dao.findByStatusIn(OPEN, RUNNING)` → empty list<br><br>**Steps:**<br>1. Call `service.markAllJobAsExpiredIfNotRunning()` | Unit | P0 | • `dao.save()` NEVER called for the WAITING job<br>• Method completes normally (active retry job must not be prematurely expired) | | - | - | - | - | UT-W6 · FR-013, UC-10 |
| 21 | RMQMessageTrackerAspect: direct SemaphoreRejectedException is NOT counted as NR failure | **Prerequisites:**<br>1. `@RunWith(MockitoJUnitRunner.class)`, `@InjectMocks RMQMessageTrackerAspect aspect`, `@Mock MetricsService metricsService`, `@Mock ProceedingJoinPoint joinPoint`<br>2. Stub `joinPoint.proceed()` throws `new SemaphoreRejectedException("throttle")`<br><br>**Steps:**<br>1. `assertThrows(SemaphoreRejectedException.class, () -> aspect.trackRabbitListenerMessage(joinPoint))`<br>2. Verify `metricsService` interactions | Unit | P0 | • `SemaphoreRejectedException` propagates (aspect re-throws — only metric is skipped, not the throw)<br>• `metricsService.markAsFailedRequest(any())` NEVER called (throttle rejection must not register as NR failure event) | | - | - | - | - | UT-W8 · FR-014 · RMQMessageTrackerAspectTest |
| 22 | RMQMessageTrackerAspect: Spring AMQP-wrapped SemaphoreRejectedException is NOT counted as NR failure | **Prerequisites:**<br>1. Class setup as #21<br>2. Stub `joinPoint.proceed()` throws `new ListenerExecutionFailedException("Listener threw exception", new SemaphoreRejectedException("throttle"), null)` (Spring AMQP wraps @RabbitListener exceptions)<br><br>**Steps:**<br>1. `assertThrows(ListenerExecutionFailedException.class, () -> aspect.trackRabbitListenerMessage(joinPoint))`<br>2. Verify `metricsService` interactions | Unit | P0 | • `ListenerExecutionFailedException` propagates (wrapper re-thrown unchanged)<br>• `metricsService.markAsFailedRequest(any())` NEVER called (.getCause() check correctly identifies throttle cause inside wrapper) | | - | - | - | - | UT-W9 · FR-014 |
| 23 | RMQMessageTrackerAspect: real exceptions ARE counted as NR failure (Fix-A regression) | **Prerequisites:**<br>1. Class setup as #21<br>2. Stub `joinPoint.proceed()` throws `new RuntimeException("MongoDB connection timeout")`<br><br>**Steps:**<br>1. `assertThrows(RuntimeException.class, () -> aspect.trackRabbitListenerMessage(joinPoint))`<br>2. Verify `metricsService` interactions | Unit | P0 | • `RuntimeException` propagates<br>• `metricsService.markAsFailedRequest(any())` called exactly once (real failures must still register — regression guard for Fix-A filter) | | - | - | - | - | UT-W10 · FR-014 · Regression: Fix-A must not suppress real failures |
| 24 | MessageRecoverer fires metricsService.markAsFailedRequest on retry exhaustion | **Prerequisites:**<br>1. Class setup as #14 (SpringAmqpConfigTest with retry props injected and `@Mock MetricsService metricsService`)<br>2. Call `SimpleRabbitListenerContainerFactory factory = config.expiryDateChangeContainerFactory(connectionFactory, configurer)`<br>3. Extract interceptor: `Advice[] chain = (Advice[]) ReflectionTestUtils.getField(factory, "adviceChain"); StatefulRetryOperationsInterceptor interceptor = (StatefulRetryOperationsInterceptor) chain[0]`<br>4. Extract recoverer: `MessageRecoverer recoverer = (MessageRecoverer) ReflectionTestUtils.getField(interceptor, "messageRecoverer")`<br>5. Build `Message msg = MessageBuilder.withBody("{}".getBytes()).build()` and `RuntimeException cause = new RuntimeException("all retries exhausted")`<br><br>**Steps:**<br>1. Call `recoverer.recover(msg, cause)` | Unit | P0 | • `metricsService.markAsFailedRequest(NewrelicConstants.EXPIRY_JOB_RETRY_EXHAUSTED_EVENT)` called exactly once<br>• No exception thrown from `recover()` (terminal handler must not re-throw) | | - | - | - | - | UT-W11 · FR-015, UC-12 · SpringAmqpConfigTest |
| 25 | updateValidTill() exits immediately (no updateMulti call) when batchIds is empty | **Prerequisites:**<br>1. Class setup as #1<br>2. Stub `mongoOperation.aggregate()` → `AggregationResults` with empty `BasicDBList` on first call<br><br>**Steps:**<br>1. Call `dao.updateValidTill(stubJob, promotionMeta)` | Unit | P1 | • `mongoOperation.updateMulti(...)` NEVER called<br>• Method exits without exception (empty batch is a valid no-op, not an error) | | - | - | - | - | UT-03 · FR-001, UC-6 |
| 26 | releaseSemaphore() calls decrement on success path | **Prerequisites:**<br>1. Class setup as #6<br>2. Stub `valueOperations.decrement("promotion_engine:expiry_job:running_count")` → `1L`<br><br>**Steps:**<br>1. Call `facade.releaseSemaphore("job-1", 100L)` | Unit | P1 | • `valueOperations.decrement("promotion_engine:expiry_job:running_count")` called exactly once<br>• No exception thrown | | - | - | - | - | UT-10 · FR-005 |
| 27 | releaseSemaphore() swallows Redis failure — does not propagate | **Prerequisites:**<br>1. Class setup as #6<br>2. Stub `valueOperations.decrement(...)` throws `new RedisConnectionFailureException("down")`<br><br>**Steps:**<br>1. Call `facade.releaseSemaphore("job-1", 100L)` | Unit | P1 | • No exception propagates (leaked counter is recoverable via hourly reconciliation; propagated exception would break retry flow) | | - | - | - | - | UT-11 · FR-005 |
| 28 | reconcileSemaphoreCounter() clamps negative input to "0" | **Prerequisites:**<br>1. Class setup as #6<br>2. Stub `valueOperations.set(any(), any())`<br><br>**Steps:**<br>1. Call `facade.reconcileSemaphoreCounter(-1L)`<br>2. Capture arg passed to `valueOperations.set()` | Unit | P1 | • `valueOperations.set("promotion_engine:expiry_job:running_count", "0")` called — NOT `"-1"`<br>• Negative running count (impossible in practice but defensive) is clamped before writing | | - | - | - | - | UT-15 · FR-007 |
| 29 | tryAcquireSemaphore() returns true when Redis counter is negative (post-restart drift) | **Prerequisites:**<br>1. Class setup as #6<br>2. Stub `valueOperations.increment(...)` → `-2L` (simulates post-Redis-restart counter drift from in-flight DECR ops)<br><br>**Steps:**<br>1. Call `facade.tryAcquireSemaphore("job-1", 100L)` | Unit | P1 | • Returns `true` (negative value ≤ max=3 → acquisition allowed; semaphore does not permanently deadlock after Redis restart)<br>• `decrement()` NOT called | | - | - | - | - | UT-16b · Risk-7 (Redis restart counter drift) |
| 30 | Single-shard reconciliation uses exact shard count (no double-counting) | **Prerequisites:**<br>1. Class setup as #13 but inject 1 shard key: `ReflectionTestUtils.setField(helper, "shardKeys", Collections.singletonList("shard-1"))`<br>2. Stub `expiryDateChangeJobDao.countByStatus(BatchStatus.RUNNING)` → `3L`<br><br>**Steps:**<br>1. Call `helper.updateExpiryDateChangeJobs()` | Unit | P1 | • `facade.reconcileSemaphoreCounter(3L)` called once — NOT `6L` (no double-count from single-entry list) | | - | - | - | - | UT-18 · FR-007 |
| 31 | expiryDateChangeContainerFactory hard-pins prefetchCount=1, concurrentConsumers=1, maxConcurrentConsumers=1 | **Prerequisites:**<br>1. Class setup as #14<br>2. Call `SimpleRabbitListenerContainerFactory factory = config.expiryDateChangeContainerFactory(connectionFactory, configurer)`<br><br>**Steps:**<br>1. Extract values: `ReflectionTestUtils.getField(factory, "concurrentConsumers")`, `"maxConcurrentConsumers"`, `"prefetchCount"` | Unit | P1 | • `concurrentConsumers == 1`<br>• `maxConcurrentConsumers == 1`<br>• `prefetchCount == 1` | | - | - | - | - | UT-20 · FR-008, NFR-005 |
| 32 | tryAcquireSemaphore() does not clamp and emits no WARN for valid configs (1 and 3) | **Prerequisites:**<br>1. Class setup as #6; attach log appender<br>2. Case 1: `setField(facade, "maxGlobalConcurrentJobs", 1)`; stub `increment()` → `1L`<br>3. Case 2: `setField(facade, "maxGlobalConcurrentJobs", 3)`; stub `increment()` → `2L`<br><br>**Steps:**<br>1. Call `tryAcquireSemaphore()` for Case 1; check result and logs<br>2. Call `tryAcquireSemaphore()` for Case 2; check result and logs | Unit | P1 | • Case 1: returns `true`; no WARN emitted (effectiveMax=1, no clamping)<br>• Case 2: returns `true`; no WARN emitted (effectiveMax=3, normal operation) | | - | - | - | - | UT-EC4c · Risk-8 (clamp guard does not activate for valid config) |
| 33 | OPEN job under 24h is NOT expired (existing 24h threshold regression after WAITING addition) | **Prerequisites:**<br>1. Class setup as #18<br>2. Build OPEN job with `attribution.lastUpdatedOn = Instant.now() minus 23 hours`<br>3. Stub `dao.findByStatusIn(OPEN, RUNNING)` → `[openJob]`; stub `dao.findByStatusIn(WAITING)` → empty list<br><br>**Steps:**<br>1. Call `service.markAllJobAsExpiredIfNotRunning()` | Unit | P1 | • `dao.save()` NEVER called (23h < 24h threshold — job still within allowed window)<br>• Regression: adding WAITING 2h loop must not have shortened the OPEN/RUNNING threshold | | - | - | - | - | UT-W7 · FR-013 · Regression: existing 24h threshold unchanged |
| 34 | MessageRecoverer logs ERROR with retry count and cause message on exhaustion | **Prerequisites:**<br>1. Class setup as #24; extract `MessageRecoverer` via same 3-step reflection<br>2. Attach `ListAppender<ILoggingEvent>` to `SpringAmqpConfig` logger<br>3. Build `Message msg` and `RuntimeException cause = new RuntimeException("downstream timeout after 10 attempts")`<br><br>**Steps:**<br>1. Call `recoverer.recover(msg, cause)`<br>2. Inspect captured log events | Unit | P1 | • At least one ERROR-level log event emitted<br>• Log message contains retry count (e.g., `"10"` matching `retryMaxAttempts`)<br>• Log message contains cause message (`"downstream timeout"`) | | - | - | - | - | UT-W12 · FR-015 |
| 35 | Regression gate: existing ISSUED static path integration suite passes unchanged after $in change | **Prerequisites:**<br>1. No changes to `PromotionExpiryDateChangeIntegrationTest`<br>2. Apply $in change to production code<br>3. Run full suite as-is<br><br>**Steps:**<br>1. Execute: `mvn test -Dtest=PromotionExpiryDateChangeIntegrationTest`<br>2. Observe test results | Integration | P0 | • All existing test methods pass (zero failures, zero errors)<br>• `impactedCount` on job equals seeded doc count in each test<br>• `validTill` on each issued coupon equals new promotion end date<br>• No test modification required | | - | - | - | - | IT-01 · FR-001, UC-1, UC-9 · Regression: RG-01, RG-02 |
| 36 | EARNED-dynamic path end-to-end: AggregationUpdate + $in updates validTill on live embedded DB | **Prerequisites:**<br>1. `@RunWith(SpringJUnit4ClassRunner.class)`, `@SpringBootTest`, extends `PromotionBaseIntegrationTest`<br>2. Create promotion with dynamic validity (DateFromParts config); insert 1 earned coupon document for `orgId=0`, `customerId=101`, record `earnedCouponId`<br>3. Create and save `ExpiryDateChangeJob`: `orgId=0L`, `status=OPEN`, `startId=new ObjectId("000000000000000000000000")`<br>4. Set Redis key to `"0"`; define `newEndDate = today+15`<br><br>**Steps:**<br>1. Call `expiryDateChangeJobFacade.changeExpiryForPromotion(job.getId())`<br>2. Reload earned coupon from `customerEarnedPromotion` collection<br>3. Reload job from `expiryDateChangeJobDao` | Integration | P0 | • `updatedCoupon.get("validTill") == newEndDate` (AggregationUpdate pipeline rewrote date correctly)<br>• `updatedJob.getImpactedCount() == 1`<br>• `updatedJob.getStatus() == BatchStatus.COMPLETED`<br>• No exception thrown | | - | - | - | - | IT-02 · FR-002, UC-1 |
| 37 | Tenant isolation: org-A job does not touch org-B coupon documents | **Prerequisites:**<br>1. Class setup as #36 (SpringJUnit4ClassRunner + SpringBootTest)<br>2. Insert 2 issued coupons for `orgId=100` (customerId=101, 102), same `promotionId="promo-500"`, `validTill=currentEndDate`; record `orgACoupon1`, `orgACoupon2`<br>3. Insert 1 issued coupon for `orgId=200`, same `promotionId="promo-500"`, `validTill=currentEndDate`; record `orgBCoupon`<br>4. Create `ExpiryDateChangeJob` scoped to `orgId=100L`; set Redis key to `"0"`; define `newEndDate = today+20`<br><br>**Steps:**<br>1. Call `expiryDateChangeJobFacade.changeExpiryForPromotion(job.getId())`<br>2. Reload all three coupon documents and the job | Integration | P0 | • `orgACoupon1.validTill == newEndDate` (org-A coupon 1 updated)<br>• `orgACoupon2.validTill == newEndDate` (org-A coupon 2 updated)<br>• `orgBCoupon.validTill == currentEndDate` (org-B coupon UNCHANGED — $in aggregation filtered by orgId=100)<br>• `updatedJob.getImpactedCount() == 2` (not 3) | | - | - | - | - | IT-03 · NFR-002 · Tenant Isolation: TI-01 |
| 38 | Counter reconciliation: inflated Redis key healed to actual RUNNING count | **Prerequisites:**<br>1. Class setup as #36; `@Autowired RedisTemplate<String,String> redisTemplate`, `@Autowired ExpiryDateChangeHelper expiryDateChangeHelper`<br>2. Ensure no `ExpiryDateChangeJob` documents exist with `status=RUNNING`<br>3. Artificially inflate Redis key: `redisTemplate.opsForValue().set("promotion_engine:expiry_job:running_count", "5")`<br>4. Assert key is set: `assert "5".equals(redisTemplate.opsForValue().get(...))`<br><br>**Steps:**<br>1. Call `expiryDateChangeHelper.updateExpiryDateChangeJobs()`<br>2. Read Redis key: `String afterValue = redisTemplate.opsForValue().get("promotion_engine:expiry_job:running_count")` | Integration | P0 | • `afterValue.equals("0")` — inflated counter corrected to actual RUNNING count of 0<br>• No exception thrown<br>• Key is not deleted — still present, value set to correct count | | - | - | - | - | IT-04 · FR-007, UC-4 |
| 39 | Multi-batch: batchSize+1 documents across two loop iterations all updated | **Prerequisites:**<br>1. Class setup as #36<br>2. Read `batchSize` from config; set `seedCount = batchSize + 1`<br>3. Insert `seedCount` issued coupon documents for `orgId=0`, same `promotionId`, `validTill=currentEndDate`<br>4. Create `ExpiryDateChangeJob`: `orgId=0`, `status=OPEN`; set Redis key to `"0"`; define `newEndDate`<br><br>**Steps:**<br>1. Call `expiryDateChangeJobFacade.changeExpiryForPromotion(job.getId())`<br>2. Count updated documents in `customerIssuedPromotion` where `validTill == newEndDate`<br>3. Reload job | Integration | P1 | • `updatedCount == batchSize + 1` (all documents updated across two loop iterations)<br>• `updatedJob.getImpactedCount() == batchSize + 1`<br>• `updatedJob.getStatus() == BatchStatus.COMPLETED` | | - | - | - | - | IT-05 · FR-001 |
| 40 | E2E: issued coupon validTill updated within 60s after updatePromotionExpiry() | **Prerequisites:**<br>1. Cluster: crm-nightly-new (dev-only — RabbitMQ consumer must be running)<br>2. File: `campaigns_auto/tests/promotion_engine/test_ExpiryJob_EndToEnd.py`, class `TestExpiryJobEndToEnd`<br>3. `setup_class`: org context and auth token available<br>4. `teardown_method`: `PeHelper.deletePromotion(promo_id)`, `PeHelper.deleteCoupon(coupon_id)`<br><br>**Steps:**<br>1. `promo_id = PeHelper.createPromotion(end_date=today+30, validity_type="FIXED")`<br>2. `coupon_id = PeHelper.issuePromotion(org_id, promo_id, customer_id=TEST_CUSTOMER_ID)`<br>3. Assert `initial_vt == today+30`<br>4. `PeHelper.updatePromotionExpiry(promo_id, new_end_date=today+15)` (triggers RMQ publish)<br>5. Poll loop (max 60s, 5s interval): check `PeHelper.getCouponValidTill(coupon_id) == today+15`; break on match; else `pytest.fail("not updated within 60s")` | Automation | P1 | • `validTill == today+15` within 60s<br>• Confirms: RMQ message consumed, job created, processed, coupon updated end-to-end<br>• Confirms: @RabbitListener still routes to correct queue after containerFactory switch (RG-04) | | - | - | - | - | AT-DEV-01 · FR-001, UC-1, UC-9, RG-04 · Dev-only · Idempotent |
| 41 | E2E: EARNED-dynamic coupon validTill updated within 60s after updatePromotionExpiry() | **Prerequisites:**<br>1. Cluster: crm-nightly-new (dev-only)<br>2. Org must support dynamic validity promotions<br>3. `teardown_method`: `PeHelper.deletePromotion(promo_id)`, `PeHelper.deleteEarnedCoupon(coupon_id)`<br><br>**Steps:**<br>1. `promo_id = PeHelper.createPromotion(end_date=today+30, validity_type="DYNAMIC")`<br>2. `coupon_id = PeHelper.issueEarning(org_id, promo_id, customer_id=TEST_CUSTOMER_ID)`<br>3. Assert `initial_vt == today+30`<br>4. `PeHelper.updatePromotionExpiry(promo_id, new_end_date=today+15)`<br>5. Poll loop (max 60s, 5s interval): same as #40 | Automation | P1 | • `validTill == today+15` within 60s<br>• Confirms: CustomerEarnedCollectionDynamic override with AggregationUpdate + $in works end-to-end on live cluster | | - | - | - | - | AT-DEV-02 · FR-002, UC-1 · Dev-only · Idempotent |
| 42 | Load baseline: 1 promotion × 200 coupons drain within 90s; capture timing for before/after comparison | **Prerequisites:**<br>1. Cluster: Nightly (`@pytest.mark.nightly`)<br>2. File: `campaigns_auto/tests/promotion_engine/test_ExpiryJob_Load.py`, class `TestExpiryJobLoad`<br>3. `teardown_class`: delete 1 promotion + 200 coupons<br><br>**Steps:**<br>1. `promo_id = PeHelper.createPromotion(end_date=today+30)`<br>2. `coupon_ids = [PeHelper.issuePromotion(promo_id, customer_id=i) for i in range(200)]`<br>3. `start = time.time()`<br>4. `PeHelper.updatePromotionExpiry(promo_id, new_end_date=today+15)`<br>5. Poll (max 90s, 5s interval): count coupons with `validTill == today+15`; break if 200; else `pytest.fail`<br>6. `print(f"elapsed={time.time()-start:.1f}s")` | Automation | P1 | • All 200 coupons updated within 90s<br>• `elapsed` printed to stdout as timing datapoint — attach before/after values to PR description as evidence | | - | - | - | - | AT-DEV-03 · FR-001, NFR-006 · Dev-only · Nightly · Idempotent |
| 43 | Load: 20 promotions × 50 coupons (1,000 total) drain within 180s under semaphore throttle | **Prerequisites:**<br>1. Cluster: Nightly (`@pytest.mark.nightly`); `maxGlobalConcurrentJobs=3` configured<br>2. `teardown_class`: delete 20 promotions + 1,000 coupons (shared with #44)<br><br>**Steps:**<br>1. `promo_ids = [PeHelper.createPromotion(end_date=today+30) for _ in range(20)]`<br>2. `coupon_ids = [PeHelper.issuePromotion(pid, customer_id=i) for pid in promo_ids for i in range(50)]` (1,000 total)<br>3. `new_end = today+15`; trigger all 20 expiry changes: `[PeHelper.updatePromotionExpiry(pid, new_end) for pid in promo_ids]`<br>4. Poll (max 180s, 5s interval): count total coupons with updated validTill; break if 1,000; else `pytest.fail("possible dead-lettering or semaphore deadlock")` | Automation | P1 | • `updated == 1000` within 180s<br>• Proves: semaphore throttle does not stall system; all 40 RMQ messages drain without dead-lettering (NFR-007) | | - | - | - | - | AT-DEV-04 · NFR-006, NFR-007, UC-2 · Dev-only · Nightly · Idempotent |
| 44 | No message loss: all 1,000 coupons in large batch have updated validTill (full scan) | **Prerequisites:**<br>1. AT-DEV-04 has completed (all 1,000 coupons confirmed drained)<br>2. Same `coupon_ids` list and `new_end` available in class scope<br><br>**Steps:**<br>1. Query ALL 1,000 coupon IDs (full scan, not sample): `all_valid_tills = [PeHelper.getCouponValidTill(cid) for cid in coupon_ids]`<br>2. Assert: `assert all(vt == new_end for vt in all_valid_tills), f"{sum(1 for vt in all_valid_tills if vt != new_end)} coupons still on old validTill"` | Automation | P1 | • `all_valid_tills` contains exactly 1,000 entries all equal to `new_end`<br>• Zero coupons remaining on original `today+30`<br>• Explicit proof: no RMQ message dead-lettered or silently dropped under peak load | | - | - | - | - | AT-DEV-05 · NFR-007 · Dev-only · Nightly · Runs immediately after #43 · Idempotent |

---

## Summary

| Field | Value |
|-------|-------|
| Total test cases | 44 |
| Passed | |
| Failed | |
| Not tested | |
| Nightly environment | |
| Test promotion ID | |
| Test date | |

---

## Notes

### PR Execution Mapping

| PR | Tasks | P0 Tests to run before merge |
|----|-------|------------------------------|
| PR-1: Semaphore + WAITING + Fix-A | B9, B10, B11, B12, B7, B13, B14, B15, B16, B8 | #6–#16, #17–#20, #21–#23 (UT-07–EC4b, UT-W3–W6, UT-W8–W10) |
| PR-2: Container Factory + Fix-B | B5, B17, B6 | #14, #24 (UT-19, UT-W11) |
| PR-3: $in Query Fix | B1, B2, B3, B4 | #1–#5, #35–#38 (UT-01–06, IT-01–04) |

### Minimum Viable Gate (P0 only — fast release)
Tests #1–#24 (P0 Unit) + Tests #35–#38 (P0 Integration) must all pass before any PR merges to staging.
Automation test #40 (AT-DEV-01) must pass on QA cluster before staging promotion.
Load tests #42–#44 run on Nightly before production rollout.

### Tenant Isolation Coverage
- Test #7 (UT-08): Global semaphore applies across all orgs — no per-org bypass
- Test #3 (UT-04): $in criteria includes orgId filter at query time
- Test #37 (IT-03): Org-B documents unchanged after org-A job execution
