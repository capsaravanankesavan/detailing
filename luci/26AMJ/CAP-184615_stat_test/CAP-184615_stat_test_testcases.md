# Stats Test Coverage — MySQL History + Summary Path

## Metadata

| Field | Value |
|-------|-------|
| Ticket | [CAP-184615](https://capillarytech.atlassian.net/browse/CAP-184615) |
| Description | Stats MySQL History + Summary Pipeline — IC/RC limit enforcement, GET API assembly, DISC_CODE_PIN upload counts, operation sequences with blueprint history |
| Module | promotion-engine / luci |
| Developer / Owner | |
| Reviewer | |
| Date | 2026-05-20 |
| Pull request | |
| Releases detailing | |
| Preconditions | |
| Security Impact | NA |

---

## Test Cases

| # | Test Case | Test Steps | Type | Priority | Expected Result | Actual Result | Dev | Nightly | Staging | Prod | Comments |
|---|-----------|------------|------|----------|-----------------|---------------|-----|---------|---------|------|----------|
| 1 | UT-01 — getHistoricalValue() when snapshot=today → summary skipped, blueprint value returned | **Prerequisites:**<br>1. `StatsHistoryServiceImpl` under test with all DAO dependencies mocked via Mockito<br>2. `StatsHistoryRegistryDao` mock: `findLastActiveByOrgId(orgId)` → returns `StatsHistoryRegistryEntity` with `snapShotDate = LocalDate.now()` (today)<br>3. `StatsHistoryDao` mock: `findHistoricalValue(...)` → returns `42L`<br>4. `StatsSeriesSummaryDao` mock: configured for `verify(..., never())` assertion<br><br>**Steps:**<br>1. Invoke `statsHistoryService.getHistoricalValue(TEST_ORG_ID, TEST_SERIES_ID, IssuedCount, DEFAULT_ENTITY_TYPE, DEFAULT_ENTITY_ID)`<br>2. Capture the returned `long` value | Unit | P0 | • Return value equals `42L`<br>• `statsSeriesSummaryDao.sumValuesForDateRange(...)` is **never** invoked (verified via `verify(dao, never()).sumValuesForDateRange(...)`)<br>• No exception is thrown | | - | - | - | - | FR-008 |
| 2 | UT-02 — isMysql() on CouponSeriesStatisticsField: RIC=true, IC=false, RRC=true | **Prerequisites:**<br>1. No mocks — pure enum state assertion on `CouponSeriesStatisticsField`<br><br>**Steps:**<br>1. Assert `RevokedIssueCount.isMysql() == true`<br>2. Assert `IssuedCount.isMysql() == false`<br>3. Assert `ReactivatedRedemptionCount.isMysql() == true` | Unit | P0 | • All three assertions pass<br>• Spot-check: `RedemptionCount.isMysql() == false` | | - | - | - | - | FR-005 |
| 3 | UT-04 — getHistoricalValue() when no registry entry → fallback to today−100, no NPE | **Prerequisites:**<br>1. `StatsHistoryServiceImpl` under test with all DAO dependencies mocked<br>2. `StatsHistoryRegistryDao` mock: `findLastActiveByOrgId(orgId)` → returns `null`<br>3. `StatsSeriesSummaryDao` mock: `sumValuesForDateRange(orgId, seriesId, IssuedCount, today−100, today−1, ...)` → returns `7L`<br><br>**Steps:**<br>1. Invoke `statsHistoryService.getHistoricalValue(TEST_ORG_ID, TEST_SERIES_ID, IssuedCount, DEFAULT_ENTITY_TYPE, DEFAULT_ENTITY_ID)`<br>2. Capture the returned `long` value | Unit | P0 | • Return value equals `7L`<br>• No `NullPointerException` is thrown<br>• `statsSeriesSummaryDao.sumValuesForDateRange(...)` is invoked exactly once (startDate = today−100) | | - | - | - | - | FR-006 |
| 4 | UT-07 — getLastRedeemTime OU entity guard: cachedStat ignored, historicalTimestamp used for OU-scoped calls | **Prerequisites:**<br>1. `MySQLCouponSeriesStatisticsReadService` under test with dependencies mocked<br>2. `isEntityLevelRedemptionConfigEnabled = true` for the org/series<br>3. `bulkFindByOrgIdAndSeriesIdsForDateRange(...)` mock → returns `CachedStat` with `lastRedeemedTime = T1` (non-zero)<br>4. `bulkGetHistoricalValues(...)` mock → returns map with `HistoricalValueKey(orgId, seriesId, LastRedeemTime, "OU", ouId) → T2` where `T2 ≠ T1`<br><br>**Steps:**<br>1. Invoke the stats read service for the series with OU-level redemption config<br>2. Call `result.getLastRedeemedTime(ouId)` → capture returned timestamp | Unit | P0 | • Returned timestamp equals `T2` (DataBricks blueprint history), **not** `T1` (stats_series_summary)<br>• Confirms OU entity guard (`redemptionEntityId != DEFAULT` → skip cachedStat, use historicalTimestamp) is in effect<br>• Note: existing `EntityLevel_LastRedeemTime_RoutesToRegularKeys` test uses empty cachedStat — does NOT catch this regression | | - | - | - | - | FR-002, FR-007 |
| 5 | IT-01 — RC limit with blueprint history: blueprint RC=14, maxRedeem=15; 1st redeem succeeds, 2nd fails 605 | **Prerequisites:**<br>1. `saveOrgDefaultProperties(orgId=0)`<br>2. `activeTable = createRollingTable(4)` (snapshot = 4 days ago)<br>3. `createStatsHistoryRegistryEntry(orgId=0, activeTable, fourDaysAgo, isActive=true, now)`<br>4. `savedSeries = saveCouponSeries(orgId, createCouponSeriesWithLimits(orgId, maxCreate=-1, maxRedeem=15))`; `seriesId = (long) savedSeries.getId()`<br>5. `insertHistoricalStatsData(orgId=0, seriesId, activeTable, ic=0L, rc=14L)`<br>6. `deleteRedisByKeyPattern(MIDNIGHT_EXPIRING_CACHE_NAME + "*")`<br>7. Issue 2 coupons to customers 101 and 102<br><br>**Steps:**<br>1. `redeemCoupon(orgId, storeUnitId, customerId=101, [coupon1], shouldCommit=true)` → `firstRedeem`<br>2. `getCouponConfigForSeries(orgId, seriesId)` → `configAfterFirst`<br>3. `redeemCoupon(orgId, storeUnitId, customerId=102, [coupon2], shouldCommit=true)` → `secondRedeem`<br><br>**Teardown:**<br>1. `statsHistoryRegistryDao.deleteByOrgId(0)`<br>2. `jdbcTemplate.execute("DROP TABLE IF EXISTS " + activeTableName)`<br>3. `deleteRedisByKeyPattern(MIDNIGHT_EXPIRING_CACHE_NAME + "*")` | Integration | P0 | • Step 1: `firstRedeem.get(0).getEx()` is null (blueprint 14 + today 1 = 15 = limit)<br>• Step 2: `configAfterFirst.getNum_redeemed()` == 15<br>• Step 3: `secondRedeem.get(0).getEx()` is not null; `errorCode` == 605; `errorMsg` == "max redeem for series exceeded" | | - | - | - | - | FR-003 |
| 6 | IT-02 — GET API assembly: blueprint IC=20, RC=8 + past-day summary → num_issued=23, num_redeemed=9 | **Prerequisites:**<br>1. `activeTable = createRollingTable(4)` + registry (same pattern as IT-01)<br>2. `seriesId = (long) savedSeries.getId()`<br>3. `insertHistoricalStatsData(orgId=0, seriesId, activeTable, ic=20L, rc=8L)`<br>4. `insertStatsSeriesSummary(orgId=0, seriesId, twoDaysAgo, "ic", 1L)`<br>5. `insertStatsSeriesSummary(orgId=0, seriesId, yesterday, "ic", 2L)`<br>6. `insertStatsSeriesSummary(orgId=0, seriesId, yesterday, "rc", 1L)`<br>7. `deleteRedisByKeyPattern(MIDNIGHT_EXPIRING_CACHE_NAME + "*")`<br><br>**Steps:**<br>1. `getCouponConfigForSeries(orgId=0, seriesId)` → `config`<br>2. `fetchTodayStats(orgId=0, seriesId)` → `todayRow`<br><br>**Teardown:** same as IT-01 | Integration | P0 | • `config.getNum_issued()` == 23 (blueprint 20 + summary 1+2)<br>• `config.getNum_redeemed()` == 9 (blueprint 8 + summary 1)<br>• `todayRow.IC` == 0 (no real operations today) | | - | - | - | - | FR-001, FR-002 |
| 7 | IT-03 — IC limit adjusted by historical RIC: effectiveIssued = (IC−RIC) + blueprint_IC; no clamping | **Prerequisites:**<br>1. `activeTable = createRollingTable(4)` + registry<br>2. Create series with `maxCreate=16`; `seriesId = (long) savedSeries.getId()`<br>3. `insertHistoricalStatsData(orgId=0, seriesId, activeTable, ic=20L, rc=0L)`<br>4. `insertStatsSeriesSummary(orgId=0, seriesId, threeDaysAgo, "ric", 3L)`<br>5. `insertStatsSeriesSummary(orgId=0, seriesId, yesterday, "ric", 2L)`<br>6. `deleteRedisByKeyPattern(MIDNIGHT_EXPIRING_CACHE_NAME + "*")`<br><br>**Steps:**<br>1. `issueCoupon(orgId, storeUnitId, customerId=101)` → `firstIssue`<br>2. `issueCoupon(orgId, storeUnitId, customerId=102)` → `secondIssue`<br>3. `getCouponConfigForSeries(orgId=0, seriesId)` → `configAfterFirst`<br><br>**Teardown:** same as IT-01 | Integration | P0 | • Step 1: `firstIssue.getEx()` is null (effectiveIssued = (1−5) + 20 = 16 = limit)<br>• Step 2: `secondIssue.getEx()` not null; `errorCode` == 626<br>• Step 3: `configAfterFirst.getNum_issued()` == 16<br>• Code comment: `// IC−RIC goes negative (−4); getSumFromCache does NOT clamp. See Q2 resolution.` | | - | - | - | - | FR-004, FR-005 |
| 8 | IT-04 — Absent registry fallback: no rolling table, no registry; summary-only IC; no NPE | **Prerequisites:**<br>1. Do NOT call `createRollingTable()` or `createStatsHistoryRegistryEntry()` for this org<br>2. Create series with `maxCreate=5`; `seriesId = (long) savedSeries.getId()`<br>3. `insertStatsSeriesSummary(orgId=0, seriesId, yesterday, "ic", 4L)`<br>4. `deleteRedisByKeyPattern(MIDNIGHT_EXPIRING_CACHE_NAME + "*")`<br><br>**Steps:**<br>1. `issueCoupon(orgId, storeUnitId, customerId=101)` → `firstIssue`<br>2. `issueCoupon(orgId, storeUnitId, customerId=102)` → `secondIssue`<br><br>**Teardown:** `deleteRedisByKeyPattern(MIDNIGHT_EXPIRING_CACHE_NAME + "*")` only | Integration | P0 | • Step 1: `firstIssue.getEx()` is null (summary IC=4 + today=1 = 5 = limit)<br>• Step 2: `secondIssue.getEx()` not null; `errorCode` == 626<br>• **Critical:** `errorCode` ≠ 500 (no NullPointerException from null registry) | | - | - | - | - | FR-006 |
| 9 | IT-07 — DISC_CODE_PIN: numTotal and num_uploaded_total combine blueprint TC/UTC with past-day summary | **Prerequisites:**<br>1. `activeTable = createRollingTable(4)` + registry<br>2. Create DISC_CODE_PIN series: `.setClient_handling_type("DISC_CODE_PIN")`; `seriesId = (long) savedSeries.getId()`<br>3. `insertStatsHistoryData(orgId=0, activeTable, [tc=800L, utc=800L, ic=0L])` via `Fixture.getStatsHistory(...)` (⚠️ verify helper exists; create if absent)<br>4. `insertStatsSeriesSummary(orgId=0, seriesId, twoDaysAgo, "tc", 200L)`<br>5. `insertStatsSeriesSummary(orgId=0, seriesId, twoDaysAgo, "utc", 200L)`<br>6. `uploadCoupons(...)` to activate the DISC_CODE_PIN series<br>7. `deleteRedisByKeyPattern(MIDNIGHT_EXPIRING_CACHE_NAME + "*")`<br><br>**Steps:**<br>1. `getCouponConfigForSeries(orgId=0, seriesId)` → `config`<br><br>**Teardown:** same as IT-01 | Integration | P0 | • `config.getNumTotal()` == 1000 (blueprint TC 800 + summary TC 200)<br>• `config.getNum_uploaded_total()` == 1000 (blueprint UTC 800 + summary UTC 200)<br>• `config.getNum_issued()` == 0 + blueprint_IC (no today ops) | | - | - | - | - | FR-009, FR-010 |
| 10 | IT-08 — upload→issue→revoke→issue with blueprint history: DISC_CODE_PIN full sequence (numTotal=549, num_issued=78) | **Prerequisites:**<br>1. `activeTable = createRollingTable(4)` + registry<br>2. Create DISC_CODE_PIN series with `maxCreate=90`; `seriesId = (long) savedSeries.getId()`<br>3. `insertStatsHistoryData(orgId=0, activeTable, [tc=500L, utc=500L, ic=70L])`<br>4. `insertStatsSeriesSummary(orgId=0, seriesId, twoDaysAgo, "ic", 10L)`<br>5. `insertStatsSeriesSummary(orgId=0, seriesId, twoDaysAgo, "ric", 5L)`<br>6. `deleteRedisByKeyPattern(MIDNIGHT_EXPIRING_CACHE_NAME + "*")`<br>7. `uploadCoupons(orgId=0, seriesId, 50)` → TC_today=50<br><br>**Steps:**<br>1. Issue 3 coupons → IC_today=3<br>2. Revoke coupon 1 → RIC_today=1, TC_today=49<br>3. Issue 1 more → IC_today=4<br>4. `getCouponConfigForSeries(orgId=0, seriesId)` → `config`<br>5. `fetchTodayStats(orgId=0, seriesId)` → `todayRow`<br><br>**Teardown:** same as IT-01 | Integration | P0 | • `config.getNumTotal()` == 549 (cachedStat TC=49 + blueprint TC=500)<br>• `config.getNum_uploaded_total()` == 549<br>• `config.getNum_issued()` == 78 (cachedStat IC=14−RIC=6=8; 8+70)<br>• `todayRow.IC` == 4, `todayRow.RIC` == 1, `todayRow.TC` == 49 | | - | - | - | - | FR-009, FR-010, FR-011 |
| 11 | IT-09 — issue→redeem→reactivate with historical blueprint RC: RRC offsets effective RC cap; num_redeemed=20 | **Prerequisites:**<br>1. `activeTable = createRollingTable(4)` + registry<br>2. Create DISC_CODE series with `maxCreate=unlimited, maxRedeem=20`; `seriesId = (long) savedSeries.getId()`<br>3. `insertHistoricalStatsData(orgId=0, seriesId, activeTable, ic=30L, rc=18L)`<br>4. `insertStatsSeriesSummary(orgId=0, seriesId, twoDaysAgo, "rrc", 5L)`<br>5. `deleteRedisByKeyPattern(MIDNIGHT_EXPIRING_CACHE_NAME + "*")`<br>6. Issue 5 coupons to customers 101–105<br><br>**Steps:**<br>1. Redeem all 5 coupons (`shouldCommit=true`) → RC_today=5<br>2. Reactivate 3 redemptions → RRC_today=3<br>3. Redeem 3 more + issue 2 new + redeem → RC_today=10<br>4. `getCouponConfigForSeries(orgId=0, seriesId)` → `config`<br>5. Attempt 11th over-limit redeem → capture `overLimit`<br><br>**Teardown:** same as IT-01 | Integration | P0 | • Steps 1–3: all redeems succeed (ex is null)<br>• Step 4: `config.getNum_redeemed()` == 20 (cachedStat RC=10−RRC=8=2; 2+18)<br>• Step 5: `overLimit.getEx()` not null; `errorCode` == 605 | | - | - | - | - | FR-002, FR-012 |
| 12 | IT-10 — upload→redeem→reactivate with blueprint TC/RC: DISC_CODE_PIN (numTotal=750, num_redeemed=13) | **Prerequisites:**<br>1. `activeTable = createRollingTable(4)` + registry<br>2. Create DISC_CODE_PIN series with `maxRedeem=20`; `seriesId = (long) savedSeries.getId()`<br>3. `insertStatsHistoryData(orgId=0, activeTable, [tc=500L, utc=500L, ic=20L, rc=10L])`<br>4. `insertStatsSeriesSummary(orgId=0, seriesId, twoDaysAgo, "tc", 200L)`<br>5. `insertStatsSeriesSummary(orgId=0, seriesId, twoDaysAgo, "utc", 200L)`<br>6. `deleteRedisByKeyPattern(MIDNIGHT_EXPIRING_CACHE_NAME + "*")`<br>7. `uploadCoupons(50)` → TC_today=50; issue 5 coupons → IC_today=5<br><br>**Steps:**<br>1. Redeem 3 coupons (`shouldCommit=true`) → RC_today=3<br>2. Reactivate 2 → RRC_today=2<br>3. Redeem 2 more → RC_today=5<br>4. `getCouponConfigForSeries(orgId=0, seriesId)` → `config`<br><br>**Teardown:** same as IT-01 | Integration | P0 | • `config.getNumTotal()` == 750 (cachedStat TC=250 + blueprint TC=500)<br>• `config.getNum_uploaded_total()` == 750<br>• `config.getNum_issued()` == 25 (cachedStat IC=5−RIC=0=5; 5+20)<br>• `config.getNum_redeemed()` == 13 (cachedStat RC=5−RRC=2=3; 3+10) | | - | - | - | - | FR-009, FR-013 |
| 13 | IT-05 — OU-level RC limit with blueprint OU-RC=2, maxOuRedeem=3; 1st redeem succeeds, 2nd fails 605 | **Prerequisites:**<br>1. Follow setup pattern from `RedeemCouponIntegrationTest#shouldValidateRedemptionPerCoupounMaxLimitForOuWithDiffTillId`<br>2. Identify `storeUnitId` → `ouId` mapping via `OrgEntityRelationsService#getOUIdForTill`; store as `targetOuId`<br>3. Create series with `entityLevelRedemptionConfigEnabled=true` + per-OU config (`maxOuRedeem=3`); `seriesId = (long) savedSeries.getId()`<br>4. `activeTable = createRollingTable(4)` + registry<br>5. Seed blueprint OU-RC: `insertStatsHistoryData(orgId=0, activeTable, [StatsHistory(orgId, seriesId, "rc", 2L, "OU", targetOuId)])`<br>6. `deleteRedisByKeyPattern(MIDNIGHT_EXPIRING_CACHE_NAME + "*")`<br>7. Issue 2 coupons to customers 101 and 102<br><br>**Steps:**<br>1. `redeemCoupon(orgId, storeUnitId, customerId=101, [coupon1], shouldCommit=true)` → `firstRedeem`<br>2. `redeemCoupon(orgId, storeUnitId, customerId=102, [coupon2], shouldCommit=true)` → `secondRedeem`<br><br>**Teardown:** same as IT-01<br><br>⚠️ Verify `storeUnitId` → `ouId` mapping in test env before seeding; silent skip if mapping absent. | Integration | P1 | • Step 1: `firstRedeem.get(0).getEx()` is null (historical OU-RC=2 + today=1 = 3 = limit)<br>• Step 2: `secondRedeem.get(0).getEx()` not null; `errorCode` == 605 | | - | - | - | - | FR-007 |
| 14 | RG-01 — testHistoricalStatsWithCouponIssueAndRedeem: IC + RC limit with blueprint+summary (orgId=123) | **Prerequisites:**<br>1. Run existing test `testHistoricalStatsWithCouponIssueAndRedeem` in `StatsSeriesSummaryIntegrationTest` without any modification<br>2. Uses `orgId=123` and `couponSeriesId=1L` (existing convention — do not change)<br><br>**Steps:**<br>1. `mvn test -pl luci -Dtest=StatsSeriesSummaryIntegrationTest#testHistoricalStatsWithCouponIssueAndRedeem` | Regression | P0 | • Test passes (green)<br>• First issue: `issuedCoupon.getEx()` == null<br>• Second issue: `failedCoupon.getEx().getErrorCode()` == 626<br>• GET API: `num_issued` == 24, `num_redeemed` == 15 | | - | - | - | - | |
| 15 | RG-02 — shouldReflectRicAndRrcWhenRevokeOrReactivateOnDifferentDay: cross-day RIC/RRC write path | **Prerequisites:**<br>1. Run existing test `shouldReflectRicAndRrcWhenRevokeOrReactivateOnDifferentDay` in `StatsSeriesSummaryIntegrationTest` without modification<br><br>**Steps:**<br>1. `mvn test -pl luci -Dtest=StatsSeriesSummaryIntegrationTest#shouldReflectRicAndRrcWhenRevokeOrReactivateOnDifferentDay` | Regression | P0 | • Test passes (green)<br>• After cross-day revoke: `yesterday_stats.IC == 3`, `today_stats.RIC == 2`<br>• After cross-day reactivation: `yesterday_stats.RC == 3`, `today_stats.RRC == 2`<br>• `num_issued` unchanged after revoke | | - | - | - | - | |
| 16 | RG-03 — shouldIssueRevokeRedeemReactivateCouponsAndValidateResponses: TC/UTC decrement on revoke | **Prerequisites:**<br>1. Run existing test `shouldIssueRevokeRedeemReactivateCouponsAndValidateResponses` in `StatsSeriesSummaryIntegrationTest` without modification<br><br>**Steps:**<br>1. `mvn test -pl luci -Dtest=StatsSeriesSummaryIntegrationTest#shouldIssueRevokeRedeemReactivateCouponsAndValidateResponses` | Regression | P0 | • Test passes (green)<br>• After 3 revokes from 1000 uploads: `numTotal` == 997<br>• `num_issued` after revoke == 22 (from 25)<br>• Revoke decrements TC in `stats_series_summary` | | - | - | - | - | |
| 17 | RG-04 — StatsHistoryServiceImplTest existing UTs: all pre-existing unit assertions pass | **Prerequisites:**<br>1. Run all existing UTs in `StatsHistoryServiceImplTest` without modification<br><br>**Steps:**<br>1. `mvn test -pl luci -Dtest=StatsHistoryServiceImplTest` | Regression | P0 | • All tests pass (green)<br>• `getHistoricalValue`, `getMysqlSummaryValue`, `bulkGetHistoricalValues` behaviour unchanged<br>• Zero regressions from new IT/UT additions | | - | - | - | - | |
| 18 | UT-03 — getLongFields() excludes RIC/RRC; getMysqlLongFields() includes them | **Prerequisites:**<br>1. No mocks — pure assertion on `CouponSeriesStatisticsField` enum static methods<br><br>**Steps:**<br>1. Call `CouponSeriesStatisticsField.getLongFields()` → capture list<br>2. Call `CouponSeriesStatisticsField.getMysqlLongFields()` → capture list | Unit | P1 | • `getLongFields()` does NOT contain `RevokedIssueCount`<br>• `getLongFields()` does NOT contain `ReactivatedRedemptionCount`<br>• `getMysqlLongFields()` contains both `RevokedIssueCount` and `ReactivatedRedemptionCount`<br>• List sizes consistent with total enum count | | - | - | - | - | FR-001 |
| 19 | UT-05 — bulkGetHistoricalValues() returns blueprint values only; no summary fan-out | **Prerequisites:**<br>1. `StatsHistoryServiceImpl` under test with all DAO dependencies mocked<br>2. `statsHistoryDao.bulkFindHistoricalValues(orgId, seriesIds, [IssuedCount])` mock → returns `{SeriesFieldKey(seriesId, IssuedCount) → 20L}`<br>3. `statsSeriesSummaryDao` mock: configured for `verify(..., never())`<br><br>**Steps:**<br>1. Invoke `statsHistoryService.bulkGetHistoricalValues(orgId, [seriesId], [IssuedCount])`<br>2. Capture returned map | Unit | P1 | • Map contains `SeriesFieldKey(seriesId, IssuedCount) → 20L`<br>• `statsSeriesSummaryDao` is **never** invoked<br>• Confirms blueprint-only split: summary range owned by `bulkFetchStatsFromCacheOrDb` | | - | - | - | - | FR-004 |
| 20 | UT-06 — getLastIssueTime: cachedStat.lastIssuedTime=T1 wins over historicalTimestamp=T2 when T2>T1 | **Prerequisites:**<br>1. `MySQLCouponSeriesStatisticsReadService` under test with dependencies mocked<br>2. `bulkFindByOrgIdAndSeriesIdsForDateRange(...)` mock → `CachedStat` with `lastIssuedTime = T1` (non-zero, e.g. `1000L`)<br>3. `bulkGetHistoricalValues(...)` mock → `HistoricalValueKey(orgId, seriesId, LastIssueTime) → T2` where `T2 > T1` (e.g. `9999L`)<br><br>**Steps:**<br>1. Invoke the stats read service for the series (standard entity, non-OU)<br>2. Capture `result.getStatistics().get(LastIssueTime).getTime()` | Unit | P1 | • Returned timestamp equals `T1` (today-path wins)<br>• Does NOT return `T2`<br>• Regression guard: `Math.max(T1, T2)` refactor would return T2 and fail this test | | - | - | - | - | FR-001, FR-002 |
| 21 | TI-01 — Blueprint seeded for orgId=0 must not affect orgId=123 registry/rolling table state | **Prerequisites:**<br>1. Org-A (orgId=0): create rolling table + registry + seed blueprint IC/RC (IT-01 setup)<br>2. Org-B (orgId=123): note current row count in `stats_history_registry WHERE org_id = 123`<br><br>**Steps:**<br>1. Execute IT-01 through IT-10 (orgId=0) in same test run as RG-01 (orgId=123)<br>2. After all new ITs complete: query `stats_history_registry WHERE org_id = 123` → row count<br>3. Query rolling tables created by new ITs; confirm no rows with `org_id = 123` | Integration | P0 | • `stats_history_registry` row count for `org_id=123` unchanged from baseline<br>• Rolling tables for orgId=0 contain zero rows for `org_id=123`<br>• RG-01 passes unmodified alongside all new ITs | | - | - | - | - | NFR-001 |
| 22 | TI-02 — Redis cache eviction in new ITs (orgId=0) must not invalidate orgId=123 cache entries | **Prerequisites:**<br>1. Verify `MIDNIGHT_EXPIRING_CACHE_NAME` key pattern is org-scoped (contains `orgId` in key segment)<br>2. Populate Redis cache entry for orgId=123 via `getHistoricalValue(orgId=123, ...)`; confirm entry exists<br><br>**Steps:**<br>1. Execute `deleteRedisByKeyPattern(MIDNIGHT_EXPIRING_CACHE_NAME + "*")` scoped to orgId=0 ops<br>2. Call `getHistoricalValue(orgId=123, ...)` → capture result<br>3. Check whether orgId=123 cache entry was evicted or still present | Integration | P0 | • orgId=123 cache entry still present (cache hit) — wildcard eviction does not cross org boundaries<br>• `getHistoricalValue(orgId=123)` returns same cached value as before eviction<br>• If key pattern is NOT org-scoped: flag as P0 isolation bug requiring `MIDNIGHT_EXPIRING_CACHE_NAME` key redesign | | - | - | - | - | NFR-002 |
| 23 | AT-DEV-01 — Pipeline integrity: num_issued and num_redeemed prove DataBricks aggregation is live; time fields ≈ now | **Prerequisites:**<br>1. `series_code = _monthly_series_code("STATS_GEN")` (e.g. `STATS_GEN_2026_05`)<br>2. `series_id, cfg, is_new = _get_or_create_monthly_series(series_code, client_handling_type='DISC_CODE')`<br>3. Capture baseline: `before_issued = api.num_issued`, `before_redeemed = api.num_redeemed`<br>4. `before_db_issued = LuciDBHelper.getCouponsIssued_Count(series_id, active=1)`<br><br>**Steps:**<br>1. Issue N=2 coupons to 2 distinct users<br>2. Redeem M=1 coupon (`shouldCommit=True`)<br>3. `_get_coupon_config(series_id)` → `after_api`<br>4. `LuciDBHelper.getCouponsIssued_Count(series_id, active=1)` → `after_db_issued`<br>5. `LuciDBHelper.getCouponRedemptions_Count(series_id)` → `after_db_redeemed` (active=1)<br>6. Assert `latestIssualTime` delta from now | Automation | P0 | • `after_api.num_issued − before_issued == 2` (delta)<br>• `after_api.num_redeemed − before_redeemed == 1` (delta)<br>• Pipeline: `after_api.num_issued == after_db_issued` (skip if `is_new`)<br>• Pipeline: `after_api.num_redeemed == after_db_redeemed` (skip if `is_new`)<br>• `latestIssualTime` non-null; `now − latestIssualTime < 5 min`<br>• `latestRedemptionTime` non-null; `now − latestRedemptionTime < 5 min` | | - | - | - | - | suiteType: smoke |
| 24 | AT-DEV-02 — Pipeline integrity: numTotal and num_uploaded_total for DISC_CODE_PIN monthly series | **Prerequisites:**<br>1. `series_code = _monthly_series_code("STATS_DCP")` (e.g. `STATS_DCP_2026_05`)<br>2. `series_id, cfg, is_new = _get_or_create_monthly_series(series_code, client_handling_type='DISC_CODE_PIN')`<br>3. If `is_new`: `queuePumpWait(series_id)` + upload 10 NONE codes<br>4. Capture baseline: `before_total = api.numTotal`, `before_utc = api.num_uploaded_total`<br><br>**Steps:**<br>1. Upload M=5 NONE-type codes<br>2. `_get_coupon_config(series_id)` → `after_api` | Automation | P0 | • `after_api.numTotal − before_total == 5` (delta)<br>• `after_api.num_uploaded_total − before_utc == 5` (delta)<br>• `after_api.numTotal > before_total` confirms today-path TC write is live | | - | - | - | - | suiteType: smoke |
| 25 | AT-DEV-06 — Combined pipeline integrity + RC limit smoke: dynamic maxRedeem, fully idempotent | **Prerequisites:**<br>1. `series_code = _monthly_series_code("STATS_SM_LMT")` (e.g. `STATS_SM_LMT_2026_05`)<br>2. `series_id, cfg, is_new = _get_or_create_monthly_series(series_code, client_handling_type='DISC_CODE')`<br>3. `current_rc = _get_coupon_config(series_id)['num_redeemed']`<br>4. Set `maxRedeem = current_rc + 1` via `_update_series_config(...)`<br>5. Issue 2 coupons to 2 distinct users (coupon_A, coupon_B)<br><br>**Steps:**<br>1. Redeem coupon_A (`shouldCommit=True`) → `firstRedeem`<br>2. `_get_coupon_config(series_id)` → `after_first`; `getCouponRedemptions_Count(series_id)` → `db_count`<br>3. Redeem coupon_B → `secondRedeem`<br>4. Reset: `_update_series_config(..., max_redeem=-1)` | Automation | P0 | • Step 1: no exception (at-limit redeem succeeds)<br>• Step 2: `after_first.num_redeemed == current_rc + 1`; pipeline: `after_first.num_redeemed == db_count` (active=1)<br>• Step 3: `secondRedeem.ex.errorCode == 605` (over-limit)<br>• Step 4: `maxRedeem` reset to -1 confirmed | | - | - | - | - | suiteType: smoke |
| 26 | AT-DEV-07 — DISC_CODE_PIN + OU-level redemption config: all four count fields + OU DB integrity across two tills | **Prerequisites:**<br>1. `series_code = _monthly_series_code("STATS_OU")` (e.g. `STATS_OU_2026_05`)<br>2. `self.ou_id = constant.config['ouId']`<br>3. `series_id, cfg, is_new = _get_or_create_ou_monthly_series(series_code)` (includes `entityLevelRedemptionConfigEnabled=True`)<br>4. If `is_new`: `queuePumpWait(series_id)` + upload 10 NONE codes<br>5. Capture baseline: `before_total`, `before_utc`, `before_issued`, `before_redeemed`, `before_ou_rc`<br><br>**Steps:**<br>1. Upload N=5 NONE-type codes<br>2. Issue M=3 coupons to 3 users<br>3. Redeem K=2 coupons via OU path — coupon_1 via `tillIds[0]`, coupon_2 via `tillIds[1]` (both map to same `ouId`)<br>4. `_get_coupon_config(series_id)` → `after_api`<br>5. `getCouponsIssued_Count(series_id, active=1)` → `after_db_issued`<br>6. `getCouponRedemptions_Count(series_id)` → `after_db_redeemed`<br>7. `getCouponRedemptions_Count_ByOu(series_id, ou_id)` → `after_db_ou_rc` | Automation | P1 | • `numTotal` delta +5; `num_uploaded_total` delta +5; `num_issued` delta +3; `num_redeemed` delta +2<br>• Pipeline: `after_api.num_issued == after_db_issued` (skip if `is_new`)<br>• Pipeline: `after_api.num_redeemed == after_db_redeemed` (skip if `is_new`)<br>• OU delta: `redemptionConfigs[ou_id].num_redeemed − before_ou_rc == 2`<br>• OU DB integrity: `redemptionConfigs[ou_id].num_redeemed == after_db_ou_rc` (query: `WHERE redemption_config_entity=0 AND redemption_config_entity_id=ouId AND active=1` — till-agnostic, aggregates both tills)<br>• If `not is_new`: `redemptionConfigs[ou_id].latestRedemptionTime` non-null (DataBricks OU time field) | | - | - | - | - | suiteType: smoke |
| 27 | AT-DEV-03 — Cross-day sequence (day-of-month parity): negative currentValue path proves no clamping regression | **Prerequisites:**<br>1. `series_code = _monthly_series_code("STATS_SEQ")` (e.g. `STATS_SEQ_2026_05`)<br>2. `series_id, cfg, is_new = _get_or_create_monthly_series(series_code, client_handling_type='DISC_CODE')`<br>3. Capture baseline API + DB counts; determine `is_odd = (date.today().day % 2 == 1)`<br>4. `pool_active = getCouponsIssued_Count(series_id, active=1)`<br><br>**Steps (even day):** Issue 5, redeem 3<br>**Steps (odd day):** Issue 3, redeem 2; `actual_K = min(5, 3+pool_active)` revokes; `actual_J = min(2, M)` reactivations; poll 90s for RIC convergence | Automation | P1 | • Even: `num_issued delta +5`, `num_redeemed delta +3`<br>• Odd: `num_issued delta == N − actual_K`; `num_redeemed delta == M − actual_J`<br>• Pipeline (odd): `after_api.num_issued == getCouponsIssued_Count(active=1)` — fails if getSumFromCache clamps negative currentValue | | - | - | - | - | suiteType: regression |
| 28 | AT-DEV-04 — RC limit enforcement: fresh series per run, 3rd redeem at limit succeeds, 4th fails 605 | **Prerequisites:**<br>1. Create fresh series per run with `maxRedeem=3`<br>2. Issue 1 coupon to a single user<br><br>**Steps:**<br>1. Redeem 3 times (`shouldCommit=True` each; issue fresh coupon + reactivate between redeems as needed)<br>2. Assert `num_redeemed == 3`<br>3. Attempt 4th redeem → `fourthRedeem`<br>4. Assert `num_redeemed` unchanged | Automation | P1 | • Redeems 1–3: no exception<br>• `num_redeemed == 3` at limit<br>• 4th redeem: `ex.errorCode == 605`<br>• `num_redeemed` still == 3 after rejection | | - | - | - | - | suiteType: regression |
| 29 | AT-DEV-05 — DISC_CODE_PIN mixed-upload (NONE + USER_ID): pool invariant + FR-014 regression gate | **Prerequisites:**<br>1. `series_code = _monthly_series_code("STATS_MIX")` (e.g. `STATS_MIX_2026_05`)<br>2. `series_id, cfg, is_new = _get_or_create_monthly_series(series_code, client_handling_type='DISC_CODE_PIN')`<br>3. If `is_new`: `queuePumpWait(series_id)` + upload 10 NONE codes<br>4. Capture baseline: `before_total`, `before_utc`, `before_issued`, `before_pool = before_total − before_issued`<br><br>**Steps:**<br>1. Upload N1=5 NONE-type codes<br>2. Upload N2=3 USER_ID-type codes (pre-assigned → immediately issued)<br>3. Issue M=3 more via API<br>4. `_get_coupon_config(series_id)` → `after_api` | Automation | P0 | • `numTotal delta +8` (N1+N2)<br>• `num_uploaded_total delta +8` (**currently fails** — FR-014 bug: USER_ID uploads not counted; regression gate)<br>• `num_issued delta +6` (N2 pre-assigned + M API-issued)<br>• Pool invariant: `(after_total − after_issued) − before_pool == 2` (N1−M)<br>• Pipeline: `after_api.num_issued == getCouponsIssued_Count(series_id, active=1)` | | - | - | - | - | suiteType: regression |
| 30 | AT-DEV-08 — History-driven RC limit: persistent STATS_RC_HIST series; latestRedemptionTime frozen after 605 | **Prerequisites:**<br>1. `series_code = "STATS_RC_HIST"` (persistent, `maxRedeem=5`, never modified)<br>2. `series_id, cfg, is_new = _get_or_create_monthly_series(series_code, max_redeem=5)`<br>3. Capture baseline: `before_rc = api.num_redeemed`, `before_time = latestRedemptionTime`<br><br>**Steps (Run 1 — is_new):**<br>1. Issue K=5 coupons to K users; redeem all 5<br>2. Assert `num_redeemed == 5`; redeem 6th → `overLimit`<br>3. Pipeline integrity check<br><br>**Steps (Run 2+ — steady state):**<br>1. Issue 1 coupon; attempt redeem → `attempt`<br>2. `_get_coupon_config(series_id)` → `after_api`; record `after_time` | Automation | P1 | • Run 1: all 5 succeed; 6th `errorCode == 605`; `api.num_redeemed == DB count`<br>• Run 2+: `attempt.ex.errorCode == 605` immediately; `api.num_redeemed >= 5`; pipeline integrity passes<br>• `after_time == before_time` (failed redemption does NOT mutate `latestRedemptionTime` — value frozen from historical DataBricks data) | | - | - | - | - | suiteType: regression |

---

## Summary

| Field | Value |
|-------|-------|
| Total test cases | 30 |
| Passed | |
| Failed | |
| Not tested | |
| Nightly environment | |
| Test promotion ID | |
| Test date | |
